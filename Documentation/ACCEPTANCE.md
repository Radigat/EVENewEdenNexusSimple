# Acceptance matrix

Evidence captured through 2026-08-05:

- The Public Contracts initial-import surface now keeps a non-collapsible,
  bold red warning visible until all initial regions and pending contract-item
  details are complete. Its German and English text states the current two- to
  three-day expectation, background and relaunch continuation, materially
  reduced app usability during the import, and ongoing optimization. Strict
  Swift formatting and both localization catalog validations pass; an isolated
  SwiftPM action builds the complete app and passes 260 tests in 21 suites.
  Light/dark appearance, VoiceOver wording and owner visual acceptance remain
  open.

- The Public Contracts detail-import performance pass adds two bounded workers,
  a global 500 ms request-start interval, 100-contract work batches and
  immediate one-shot response-cache release. Expired contracts are pruned and
  known detail-free Courier/Loan contracts retain `not_applicable` instead of
  consuming ESI error budget. Nineteen focused Contract tests pass; complete
  isolated SwiftPM and unsigned native Xcode actions each pass 259 tests in 21
  suites. A long live import plus CPU, bandwidth and RSS observation remain
  owner acceptance.
- The program-wide consolidation pass inventoried the complete Swift package,
  app target, 21 test suites, both localization catalogs and the active native
  Xcode project. SwiftPM and unsigned native Xcode actions each build the app
  and pass 252 tests. The nested `EVEStaticDataKit` package passes 5 tests.
- Runtime and cross-domain repairs now discard non-target stations before
  retaining per-type regional depth, resolve Blueprint Contract offers through
  one bounded exact-type bulk snapshot, keep missing offers unresolved when the
  index is partial/stale/failed, reject stale cancelled Contract searches, and
  avoid secondary migrations or background ESI work when the primary SwiftData
  container is unavailable. German and English localization catalogs contain
  no duplicate keys and both pass `plutil` validation.

- Sale price and sold quantity are directly editable in both the five-row
  Planner preview and complete Production Overview. A quick not-sold/full-sale
  control retains numeric partial sales, and Real Profit derives from the
  persisted sale price and sold quantity. The focused calculation test plus the
  complete SwiftPM and native Xcode runs pass; owner interaction acceptance
  remains open.
- Planner input is checked continuously without a redundant Validate action.
  Focused planner coverage now proves explicit produce/buy/warehouse choices
  for eligible intermediates, buy/warehouse choices for raw inputs, shortfall
  purchases and a warehouse countervalue derived from the same weighted
  full-demand Main Hub unit price without double counting. A reaction fixture
  additionally proves that buying an intermediate removes its reaction inputs
  and job while retaining only a raw quantity that the final product also needs
  directly. Only produced branches retain their installation jobs.
- `swift test` with writable scratch/module caches and the unsigned native
  Xcode test action: 252 tests in 21 suites pass after full app builds. The
  coverage includes Main Hub persistence, complete-depth make-or-buy decisions,
  logistics and the existing 10,001-record Warehouse fixture.
- Strict Swift formatting passed for the focused Main-Hub performance source
  and test files other than the shared `MarketModels.swift`. The recursive
  repository lint still reports unrelated in-progress formatting in existing
  sections of `EVEMultibuyExport.swift`, `MarketModels.swift` and
  `ContentView.swift`. The current native Xcode project completed a separate
  unsigned Debug test build with `CODE_SIGNING_ALLOWED=NO`.
- A signed native Xcode build completed with the Profile navigation guard and
  shared SSO-session cache. The current local identity is ad-hoc
  `Sign to Run Locally`, so Data Protection Keychain entitlement behavior still
  requires an Apple Development signing identity for owner acceptance.
- `swift test --package-path Packages/EVEStaticDataKit`: 5 tests passed.
- `xcodebuild ... test CODE_SIGNING_ALLOWED=NO`: native app, framework,
  package and test targets built and the test action passed.
- The vendored package has the same file content as the
  `eve-static-data-kit` asset.
- Live SDE activation installed Tranquility build `3451778`; the activated
  catalog contains 48 categories, 1,610 groups, 52,822 types, 5,082
  blueprints, 19,137 activities, 36,499 materials and 6,329 products.
- A read-only production-recipe audit of that active catalog found 4,155
  unique published manufacturing/reaction products, zero duplicate published
  recipes, zero nonpositive outputs or times, and zero published recipes with
  unresolved materials. Tungsten Carbide resolves to the published formula
  `46207` with 10,000 output per run; the unpublished test formula is excluded.
- A read-only live facility query against that activated package resolved 14
  structure definitions, 10 compatible Standup service modules and 118
  compatible Manufacturing, Reaction, Invention, Copying, ME Research and TE
  Research rigs. The Research Lab exposes Copying, ME Research and TE Research;
  the Reprocessing Facility resolved 50% ore and 80% gas yield multipliers;
  Raitaru resolved as Medium with 1% ME and 15% TE.
- Public ESI system-detail acceptance resolved Jita/The Forge at
  `0.9459131360` as Highsec, Haine/Essence at `0.4364712536` as Lowsec, and
  B9E-H6/Immensea at `-0.3654290438` as Nullsec.
- The owner completed EVE SSO for six personal characters through the
  registered `http://localhost:52722/callback`.
- A post-fix live refresh recorded `fresh` for all six characters in all ten
  stored domains: skills, standings, blueprints, assets, industry jobs, open
  orders, order history, wallet balance, wallet journal and wallet
  transactions.
- At 11:07 UTC on 2026-07-31, CCP's public Statuspage summary reported Game
  Server and Tranquility `under_maintenance`, while Login/SSO and ESI both
  remained `operational`. This is live service evidence, not proof of the
  banner's visual owner acceptance.

| Area | Automated technical evidence | Current EVE live evidence | Owner operating acceptance |
| --- | --- | --- | --- |
| Program-wide integration and startup | pass: SwiftPM and unsigned native Xcode each build the app and pass 252 tests in 21 suites; the nested static-data package passes 5 tests; focused changed Swift files pass strict `swift-format`; both localization catalogs validate and have no duplicate keys; static checks found no `try!`, `fatalError`, `DispatchQueue.main.sync`, `Thread.sleep`, parameterless `.wait()` or tracked TODO/FIXME markers in Swift sources | current official ESI overview, cache/pagination, rate-limit and February 2026 market-order guidance were reviewed; no private endpoint or production data was changed | pending: launch once with the normal store and once against a deliberately unavailable test store, then confirm the recovery screen starts no secondary migration or background network activity; long-session CPU/RSS, offline/sleep-wake behavior and complete visual/VoiceOver acceptance remain owner work |
| SDE lifecycle and queries | pass: streamed staging, package-derived reaction rules, activation, backup, query and rollback fixtures; active pointers reject traversal/tampering, the confirmed preview must exactly match a fresh preview, schema review is an explicit UI gate, and four focused tests distinguish current, newer, schema-changing and stale-metadata/downgrade cases; the full 116-test SwiftPM and unsigned native Xcode actions pass | pass: the active local pointer is build `3451778`; on 2026-07-31 CCP's official latest metadata also reported build `3451778`, released at `11:07:15Z`, while the schema changelog's highest `afterBuildNumber` was `3407448` | active build and catalog counts were previously observed in the running app; pending re-check: enter a valid owner contact, press **Check for SDE update**, navigate away and back, confirm the contact remains, and verify the UI shows active/official `3451778` plus schema boundary `3407448` |
| SSO and token lifecycle | pass: PKCE, exact callback binding, one-time/expiring state, replay and duplicate-query rejection, mixed RSA/EC JWKS, bounded token/JWT responses, local disconnect, and boundary tests; refresh tokens are Keychain-only; one shared SSO actor reuses access-token leases; generic authorization stores under the verified character ID and expected-character rejection remains covered at the core boundary; Data Protection Keychain has a secure legacy fallback | pass: six real character grants completed with the dedicated client registration and callback | pending re-check: use **Authorize or add character** for each character whose scopes need renewal and confirm that existing characters update while a new character is inserted; then verify the destructive local disconnect flow. EVE still needs one consent per character. First legacy access may request approval once per character; stable Apple Development signing is required to accept the prompt-free Data Protection path |
| ESI paging/cache/errors | pass: bounded response/page/path handling, inconsistent pagination rejection, non-retried generic POST, character-partitioned private validators and response bodies, fresh-response reuse, disconnect purge, and LRU eviction at 512 responses or 64 MiB; the universe-name index uses at most four concurrent batches and survives cancellation of a waiting query without a restart; the exact three-character `B9E` prefix finds `B9E-H6`; region/security detail chain, ACL-checked name search, used-location structure discovery and accessible/forbidden asset-structure name resolution remain covered | the preceding ten-domain refresh predates structure-name enrichment and does not prove current Player Structure coverage; the optimized universe-index build was not rerun live in this turn | pending: reauthorize characters missing `esi-universe.read_structures.v1`, run **Sync all**, then repeat a long-running multi-character session while observing app RSS and ESI error-limit headers |
| EVE service availability and sync presentation | pass: three focused tests decode the official component names, keep Game Server maintenance separate from operational Login/SSO and ESI, tolerate an unknown future component state and apply the fallback window in UTC; the full 116-test SwiftPM and native Xcode actions pass; connection progress and incomplete-domain summaries compile in both app targets | pass: the redacted read-only Statuspage response at 11:07 UTC matched the maintenance fixture; no credential or private ESI data was used | pending: observe the global German/English banner during and immediately after a real downtime, then authorize and synchronize one character to confirm the browser, SSO and ESI phases plus incomplete-domain wording |
| Industry system indices | pass: all current activity values are preserved and sorted while unknown future raw activities remain displayable | pass: public `/industry/systems` returned 5,485 systems; the Jita record contained manufacturing, invention, reaction, copying, ME research and TE research | pending visual confirmation beneath selected Profile systems |
| Blueprint BPO/BPC semantics | pass: endpoint quantity semantics remain separate from manual planning assumptions; top-level input accepts BPC consumed-copy cost or an owner-allocated BPO share, preserves the kind and treatment per line, reports missing costs, and includes entered costs in profit/ROI | pass: personal blueprint snapshots are `fresh` for all six characters; no market price is inferred for a blueprint instance | pending owner check of BPC and BPO entries against an actual plan; automatic BPC valuation and a default BPO amortization policy remain intentionally absent |
| Blueprint inventory and research replacement estimate | pass: the 101-test SwiftPM run covers all-character portfolio ownership and source states, BPO/BPC researchability, official level multipliers, separate ME/TE facility contexts, cumulative costs, current/remaining totals, unresolved adjusted prices and blocking special research materials; the asymmetric ME 9 / TE 14 fixture proves independent reached-level markers, and stale asynchronous quote completions are rejected after selection changes; SwiftPM and native unsigned Xcode test actions pass | owner screenshot 2026-07-31 showed a `fresh` Providence BPO at ME 9 / TE 14; its displayed current-research total matched the separate ME-9 and TE-14 cumulative values, while the former shared Level marker misleadingly stopped at 7 | pending after the marker fix: switch repeatedly between BPOs with different ME/TE values and confirm the ME-target and TE-target checkmarks update independently; compare one ordinary BPO's levels 1–10 plus current/remaining totals with the in-game research window under the same systems, facilities and taxes; verify a BPC remains read-only |
| All items, production warehouse, Corporation hangars, targets and reservations | pass: prior exact-location, nested-container, ship-descendant, target and 10,001-item coverage remains; new deterministic fixtures prove that Corporation Assets are rejected before transport when the scope is absent, a non-Director remains forbidden without an empty snapshot, the assets endpoint follows all reported pages, division names and `CorpSAG` flags survive mapping, and two Directors of the same Corporation cannot double-count its quantity; one shared persisted switch feeds All items, Warehouse and Planner while Corporation remains a separate owner type | official current OpenAPI requirements were reviewed on 2026-08-02; no real Director was reauthorized and no private Corporation asset payload was requested | pending owner acceptance: under Characters use **Update permissions** for one Director, synchronize, enable **Corporation hangars anzeigen**, compare all seven visible divisions and quantities with EVE, then disable the switch and confirm Corporation rows and their Planner availability disappear while personal assets remain unchanged; repeat Warehouse ship/container exclusions and window resizing |
| Personal wallet overview | pass: focused tests cover required scope, character endpoint, authenticated request, available-value aggregation, missing values and stale portfolio state; SwiftPM and native Xcode test actions pass | underlying wallet-balance snapshots were `fresh` for all six characters at ESI compatibility `2025-12-16`; the new dedicated refresh command was not rerun against live ESI | pending: open Wallet, refresh all, compare total with each character row and check partial/forbidden messaging |
| Main Hub depth pricing, Home Hub, Trading Locations and trader fees | pass: Jita remains the default; the persisted Main Hub is restricted to Jita, Amarr, Dodixie, Rens or Hek and supplies all Planner purchase quotes and inbound logistics; the independent Home Hub accepts any configured NPC station or Player Structure; a legacy Home Hub now presents a prefilled ESI Player Structure replacement directly below its picker and replaces the existing configuration in place, preserving its UUID, role, trader and fee evidence while merging an already configured duplicate; public station search resolves station-owner/faction context, authenticated Player Structure search is not restricted to production systems, and production Tatara/Sotiyo entries are no longer proposed as Trading Locations; each location persists its own trader, sales tax, broker-fee evidence, skill levels, standings and source state | deterministic replacement coverage passes together with the full 176-test SwiftPM and unsigned native Xcode actions; official SSO, ESI overview/search and structure-resolution guidance were reviewed; no profile values were changed through the running app and no private live ESI data was requested | pending owner check: reauthorize the intended structure-search character if prompted, use the new resolver beneath the existing UALX-3 Home Hub, save/reopen Profile, confirm its trader and fees remain, and compare the resulting structure ID with EVE; then confirm the Player Structure card reports its owner-defined broker fee as unavailable rather than zero |
| Moon-material purchase analysis | pass: the active-SDE query returns only published members of `Moon Materials`; deterministic tests prove the inclusive +10% boundary, summed sell volume, six exact mappings and green/yellow/red dense ranking; NPC Moon requests now ask for sell orders only while retaining one bounded request per material instead of downloading a complete regional order book; unchanged completed results are reused inside ESI's five-minute market-cache window, while Profile, authorization, client-ID or SDE changes invalidate reuse; added fixtures prove the sell-only query contract, explicit Home-Hub replacement, and that Player Structure orders use the authorized paginated structure endpoint, reject a missing scope before network access, map the supplied system and filter unrequested types; authorization, ACL, missing/replaced structure and rate-limit failures remain unavailable rather than becoming empty markets; complete SwiftPM and unsigned native Xcode actions pass 176 tests in 19 suites | current official OpenAPI confirms `/markets/structures/{structure_id}/` requires `esi-markets.structure_markets.v1`; CCP's 2026-02-24 guidance confirms a five-minute market cache and no benefit from faster polling; a read-only 2026-08-02 comparison found one type-filtered Forge page versus 404 pages for the complete regional book. Local inspection still found Jita `60003760` as Main Hub, UALX-3 as a legacy Home Hub without `locationID`/`solarSystemID`, and none of 14 stored authorization snapshots carrying the new market scope | pending owner steps: reauthorize at least one character with the Player Structure market scope; in Profile use the new resolver beneath the existing UALX-3 Home Hub, select the matching ESI result and save; then refresh Moon analysis. Confirm repeated refresh behavior, Jita/UALX badges, UALX-3 and C-J6MT orders plus +10% quantities against EVE, and resizing/color/contrast. Live private structure-order retrieval and visual acceptance are not yet claimed |
| Cross-region Market Browser | pass: five focused deterministic tests cover all-region composition, one-region failure without losing successful orders, complete seller/buyer field mapping, regional ESI update provenance, location/system/security resolution, structure-name enrichment, Highsec/Lowsec/Nullsec and location filtering, EVE-client display rounding including the small-positive exception, exact best-volume 5% weighting, outlier exclusion, active volumes and gross average margin; each location row places the colored security value before the station or structure name, omits the duplicate standalone system name and continues to sort alphabetically by location; route calculation and jump counts remain absent; the complete SwiftPM and unsigned native Xcode actions each pass 212 tests in 20 suites | read-only current checks on 2026-08-01 confirmed that ESI publicly returns Ashab's security status, publicly lists Player Structure IDs, rejects unauthenticated structure-detail requests, and cannot accept long Player Structure IDs in the 32-bit universe-name endpoint; on 2026-08-02 the current official EVE developer guide confirmed the one-decimal display rule, the `0.0 < x < 0.05` exception and all eleven security-status colors; EVE Tycoon's current API documentation confirms its structure/system metadata and 5%-volume statistic contract; no real token, connected-character structure lookup, live all-region request or running-app comparison was performed | pending: refresh Ark or another item with a currently authorized character carrying `esi-universe.read_structures.v1`, compare Player Structure names, security values/colors, 5%-average metrics and Highsec/Lowsec/Nullsec filters with EVE Tycoon and EVE, resize both tables, and verify partial-region plus inaccessible-structure diagnostics; ESI regional `Last-Modified` must not be interpreted as a guaranteed per-order edit time, a visible public order does not prove docking access, and active-order volume is not completed-sale history |
| Main Hub manufacturing opportunities and production tree | pass: the calculator retains the complete saved candidate list before filtering, starts text search at three characters, fills the parent width, keeps Warehouse/Main-Hub/Shopping visible before item selection and top-aligns short lists. Candidate name and chevron now open an on-demand recursive reference tree; its target quantity can be recalculated without rescanning the catalog. Every producible intermediate expands independently, Build/Buy choices are applied to a separate selected plan, and stock is consumed before only the shortfall is built or bought. Exact production-facility stock is green when complete, yellow when partial, red when factually absent and neutral when unavailable. Blueprint evidence distinguishes owned BPC, owned BPO, copy and invention availability at the configured activity locations plus locally indexed public-contract offers. Blueprint offers are now loaded once through exact type IDs; missing offers remain unresolved unless the complete all-region Contract index is fresh. Inbound purchases are costed from the configured Main Hub to their consuming facility and top-level products are returned to the Main Hub. Focused fixtures cover recursive parent linkage, stock-first shortfalls, full/partial/missing coverage, exact-location BPC evidence, Contract-coverage uncertainty and round-trip logistics; complete isolated SwiftPM and unsigned native Xcode actions each pass 239 tests in 21 suites after full app builds | the supplied EVE Guru image and linked video informed the three-level layout. Read-only inspection of the user's earlier populated Jita snapshot confirmed `3,969`: complete manufacturing products with at least one active buy or sell order at that exact Main Hub, not all SDE items and not completed sales. The demand ledger remains a conservative exact-hub lower bound. Blueprint contract prices may represent bundles and corporation asset rows cannot prove BPO/BPC kind, runs, ME or TE; such facts remain unavailable. No explicit transfer edge yet costs a built intermediate moved between different reaction and manufacturing facilities. No new long live scan, private ESI comparison or running-app visual acceptance was performed | pending owner check: refresh and relaunch to confirm saved-list restoration; exercise a deep mixed manufacturing/reaction tree, change quantity and several Build/Buy decisions, and compare stock, market depth, jobs and both transport legs with EVE and the hauling provider. Confirm BPC/BPO/copy/invention location evidence, unavailable states, resizing, keyboard and VoiceOver behavior. Creating/persisting jobs, attributable copy/invention/research or bundled-contract costs, and explicit cross-facility intermediate transfers remain later work |
| Background Main-Hub update performance | pass: catalog analysis now runs away from the main actor with cooperative cancellation; the completed scan retains a compact per-type best-price summary instead of the complete regional order book, removes only the consumed bulk-market response family from the ESI memory cache, and releases the decoded demand ledger after each durable read/write. Production-tree pricing requests complete depth only for its required types, and each regional type result discards non-target stations before retention. A versioned one-time migration pauses the former implicit Main-Hub schedule until the owner explicitly resumes it; the visible status control can pause or resume it again. Focused cache-purge, compact-summary and station-retention fixtures pass, and complete isolated SwiftPM plus unsigned native Xcode actions each pass 239 tests in 21 suites | read-only local evidence before the change found Main-Hub automation enabled, a 26 MiB demand file with a `649,184`-order comparison baseline and a 12 MiB opportunity snapshot with `3,969` rows. Public Contracts automation was already disabled in its local SQLite settings. The app process was not running, so no live before/after CPU or RSS result is claimed | pending owner check: launch the updated app and confirm Main-Hub status starts paused, manual refresh remains usable, the window remains responsive during calculation, and CPU/RSS return near idle after completion. Resume automatic Main-Hub updates only if desired, then observe one six-hour/startup-delayed cycle and a long session; running-app layout, keyboard and VoiceOver acceptance remain open |
| Public Contracts all-region index | pass: seventeen focused deterministic tests prove current JSON decoding when optional `for_corporation` is absent, transactional migration of the legacy SQLite column with contract/item retention and preserved unknown values, stable first-import completion across later refresh failures, retry scheduling for failed initial regions, the three-error schema circuit breaker, exact-type bounded bulk lookup, and rejection of stale/partial/failed search coverage, in addition to active local item search, `Ark` versus `Arkonor` disambiguation through `Ship` and `Jump Freighter`, offered/requested separation, cache-expiry reuse, complete multi-page item retrieval, rate/error-limit behavior, persisted automation safety windows and location-name rules. Complete isolated SwiftPM and unsigned native Xcode actions each pass 239 tests in 21 suites. The app target compiles with a permanently visible critical region-error summary, detailed first-import/fresh/error/retry metrics, persistent full-row disclosures, a responsive two-column result grid, Region/Location/category/group/type/expiry/amount metadata and no visible Contract ID | the official OpenAPI reviewed on 2026-08-03 marks `for_corporation` as optional, and one bounded Aridia response omitted it; earlier read-only sampling found 35 public-contract pages in The Forge and the implementation respects cache, pagination, error-limit, `Retry-After` and current rate-limit headers. No complete live all-region import, authorized Player Structure-name lookup, private token or migration of the owner's production database was used for acceptance | pending: start the updated app and confirm its automatic continuation migrates the existing index without data loss, advances the remaining initial regions after their safety times, and preserves completion if a later refresh fails; quit/relaunch during its item queue and use safe stop to confirm it remains paused across another relaunch. Compare `Ark` + `Ship` + `Jump Freighter` plus NPC and Player Structure wording with the in-game search; verify collapsed diagnostics, one-/two-column resizing, keyboard and VoiceOver. Contract pauses do not globally block other ESI commands, but a shared CCP incident can affect them; CCP may change limits, so no absolute non-blocking guarantee is claimed |
| Catalog-wide reaction profitability | pass: active SDE queries return one complete published definition per reaction formula; deterministic fixtures cover 100-run material/production scaling, verified facility material/time modifiers, EIV installation cost, input logistics, direct-purchase logistics and total, complete make-or-buy value, immediate-sale spread, unavailable market depth and blocking missing packaged volume; both alternatives use the Planner's shared SDE-volume, collateral, contract-splitting and per-contract rounding routine; formula-specific 30-day job limits retain the one-run exception, expose required job counts, and lowest-value sorting puts losses before gains and unavailable values last; alternate-hub pricing filters its own station and uses bounded order requests; every reaction name is a separate accessible clipboard action with localized success/failure feedback, while the rest of the result row remains the calculation disclosure; complete SwiftPM and unsigned native Xcode actions each pass 212 tests | read-only local build `3453885` contains 119 complete published reaction formulas with 360-, 10,800- and 21,600-second base durations; the running German app loaded all `119 / 119` rows from current Jita depth with configured Tatara/B9E-H6 basis, displayed the logistics-aware calculation explanation, and exposed separate accessible buttons for row expansion and name copying. Activating `Reaktionsname kopieren` for `Pure Synth X-Instinct Booster` left its row collapsed, and pasting into the reaction search produced that exact name, proving the clipboard payload; the short-lived popover and all three newly added detail labels were not captured together in the delayed accessibility snapshot | pending owner comparison of representative biochemical, composite, hybrid and alchemy rows including both logistics alternatives against the EVE market, reaction window and transport provider; visually confirm all three logistics detail rows, the localized copy popover plus keyboard and VoiceOver operation; repeat at another hub and verify rate-limit/cache behavior in a longer session; live values remain time-bound |
| Industry graph and costs | pass: recursion, Want-to-runs conversion, job-wide ME/TE rounding, multiplicative facility modifiers and incomplete-cost blocking; every producible component, intermediate reaction and base reaction receives an exact-demand make-or-buy analysis; build cost uses batch-rounded runs and output, direct inputs, installation and inbound logistics; buy cost requires full Main Hub sell depth and inbound logistics; deterministic fixtures prove both recommendation directions and the displayed absolute saving; the purchased-reaction fixture proves that the selected intermediate is the graph leaf, its reaction inputs and job disappear, and a shared raw material retains only its independent direct demand | the running app compiled and the existing saved Planner opened with the new Main Hub wording; its pre-change stored snapshot was deliberately not recalculated, so no live analysis card was claimed | pending owner comparison of one newly recalculated component and reaction card against in-game market/job windows plus visual resizing through one-, two- and four-column layouts |
| Logistics costing | pass: SDE packaged-volume lookup, Main-Hub-to-production routing, deterministic whole-item first-fit-decreasing allocation, automatic contract splitting, maximum of volume charge versus 0.5% replacement collateral and per-contract million-ISK rounding; separate comparison legs transport either the bought component or build inputs, while missing tariff, volume, collateral or destination makes the recommendation unavailable | active SDE build `3451778` remains the configured volume source; no live provider calculator value was used in this implementation turn | pending: enter the provider's current ISK/m³ rate, recalculate a plan and compare one actual shopping-list contract plus both make-or-buy logistics alternatives with the provider calculator |
| Planner continuity, procurement, build/shopping lists and Production Overview | pass: successful plans persist source preferences; changing one material source now triggers the normal recalculation immediately, while `MakeOrBuyRecommendationApplication` changes only available component/reaction recommendations to Produce or Buy, preserves raw-material, unrelated and unavailable choices, binds applied purchases to the Main Hub, and sends the resolved preferences through the same path; each resulting job retains product, activity, runs, output, ME/TE, top-level status, facility and installation cost for the build list; `EVEShoppingList` supplies both the exact expanded Market rows and Multibuy export selection, retains purchase quantity and quote coverage, and makes the purchase total unavailable when any row lacks complete sell depth; protected warehouse targets remain intact | focused planner and shopping-list tests plus the app target compile pass; prior read-only running-app inspection confirmed the German recommendation summary and accessible **Empfehlungen übernehmen und berechnen** button, but the immediate individual source update and new Market disclosure have not yet been visually accepted | pending owner check: switch Hyperfluorite or another reaction intermediate from Produce to Buy and confirm its reaction inputs disappear without a second action; then activate the recommendation command, open Market, compare every displayed item and quantity with the copied Multibuy payload, and compare the resulting build list, single Main Hub shopping list and inbound logistics with EVE/provider data |
| Production basis and facility selection | pass: focused tests cover ME-before-TE, region/security resolution and inheritance, legacy profile review, SDE structure/service/Science-rig dogma, service compatibility, size-compatible rigs, slot caps, manual assignment, automatic reaction-refinery selection, system-bound Invention/Copying/ME/TE Research selection, one Sotiyo serving Manufacturing, Invention and Copying simultaneously through its installed modules, and an optional trader selection so a character-free profile remains savable; system pickers keep the selected value as status while presenting an empty search placeholder; the complete 108-test SwiftPM run passed after a full app build, and the unsigned native Xcode test action also passed | live ESI verified one Highsec, Lowsec and Nullsec system; read-only live SDE build `3451778` resolved 14 structures, 10 service modules and 118 Manufacturing/Reaction/Science rigs; no new live service-module evidence was collected because ESI does not expose fitted/online modules | pending visual re-check in the running Profile page: save language and basic settings before adding a character, confirm each system field starts empty and retains the selected system below it, then assign one structure location, add Manufacturing plus Invention/Research service modules, confirm the automatic-activity summary and verify that the same structure is selected independently for Manufacturing, Invention and Copying; also check Reprocessing yields, automatic Tatara/Athanor and all four Blueprint-activity choices; facility tax remains manual and Thukker rigs remain `needsReview` |
| Invention skill matrix | pass: every connected character remains a column; missing snapshots stay unknown; fresh missing skills are zero; deterministic broad-readiness ranking | active SDE contains 45 published Science skills | pending owner review of all character columns and full-row disclosure control; exact blueprint-specific invention chance remains outside v1 |
| SwiftUI accessibility | app target builds; `FullWidthDisclosureButton` is the single full-row interaction routine for reaction results, planner details/warnings/manual stock, warehouse locations/owners, character scopes, Profile scheduling, Public Contracts safety explanations and the skill matrix; it exposes expanded/collapsed state while keeping nested actions separate. The separate Public Contracts initial-import warning is deliberately non-collapsible and combines its icon and bold red text into one accessibility element; Wallet controls and dirty Profile navigation remain present | not applicable | pending owner check of every migrated disclosure and the initial-import warning plus manual VoiceOver and keyboard pass |
| Ravworks/Excel comparison | the 21 main columns and their derived formulas are represented by `StoredProductionOverviewRow` plus `ProductionOverviewCalculation`; focused tests cover the spreadsheet equations and unknown-cost propagation | `EVE-indu- Delve.xlsx`, worksheet `Produktionsübersicht`, inspected 2026-07-30; no live EVE call required | pending visual and value comparison in the running app |

Passing technical tests does not imply a live-service or owner acceptance.
The rows above mark those layers independently; no token, authorization code,
character identifier or private ESI payload is retained as acceptance evidence.

## Market Settings redesign, 2026-08-02

| Area | Automated evidence | Live/owner evidence still required |
| --- | --- | --- |
| Shared market roles | deterministic fixtures persist an arbitrary ESI-resolved NPC station as Main, allow the same location as Home without a logistics leg, persist a Player Structure as Coalition, reject an NPC Coalition selection, and prove that an unassigned legacy Jita candidate is not a market | add and save one real NPC station plus the intended Coalition structure, reopen the app and confirm all role pickers and fee attribution |
| Moon and Reactions consumers | the app target builds with Moon columns sourced only from Main, Home and Coalition; identical role locations are deduplicated, and Reactions selects one role hub, including the authenticated structure-market path and explicit unavailable states | compare one NPC and one accessible structure in both screens with current EVE orders; confirm no unassigned Jita column appears and an inaccessible structure stays unavailable |
| Public Contracts isolation | the manual-start regression proves a user refresh bypasses the local automatic safety date; transport cache and rate/error limiting remain unchanged and the public importer uses no character or Corporation scope | start, stop and manually restart a real partial import; separately deny Corporation Assets and confirm the contract import continues |

The fixed Moon-market list was removed on 2026-08-03. The isolated SwiftPM and
unsigned native Xcode actions both passed 216 tests in 20 suites. Running-app
owner acceptance for this cleanup remains separate and open.
Running-app layout, private ESI data and EVE-client comparison remain separate
owner-acceptance work.

## Moon analysis image export, 2026-08-02

| Area | Automated evidence | Owner evidence still required |
| --- | --- | --- |
| Complete PNG export | isolated SwiftPM and unsigned native Xcode actions each pass 192 tests in 20 suites after building the app with a dedicated non-scrolling export view that renders every configured hub and every Moon material, retains price-rank and source-state presentation, and adds the local export date and time to both the image heading and suggested PNG file name | use **Export as image** with the intended live hub set, save the PNG, open it at full size and confirm the rightmost hub, final material row, localized timestamp, colors and text remain readable |

This export uses the analysis snapshot currently visible in the app. It does
not trigger another ESI refresh and does not replace missing or inaccessible
market values.

## Dashboard and industry jobs, 2026-08-04

| Area | Automated evidence | Owner evidence still required |
| --- | --- | --- |
| Startup and industry jobs | Dashboard is the first sidebar item and initial selection. The linked Industry Jobs table provides All/Active/Ready/Delivered/Cancelled plus six activity filters, an optional delivered-history view, character, product/blueprint, runs, status, remaining/completion time and Station/Structure. The persisted ESI DTO remains backward-compatible while retaining current `station_id`, `product_type_id`, timing and success fields. NPC stations are resolved publicly; Player Structures use the already authorized structure-detail scope, and unresolved names remain explicit. A cached `active` job whose `end_date` has elapsed is projected as ready immediately instead of disappearing until the next sync; `paused` remains paused because facility downtime can suspend progress. Deterministic tests cover both transitions plus current-schema and legacy-snapshot decoding | synchronize every intended character once, relaunch, and compare filters, active/capacity totals, finished jobs, next completions and representative NPC/Player-Structure names with the EVE Industry window; verify narrow-window table behavior, keyboard and VoiceOver. Player Structures hidden by ACL or missing scope must remain visibly unresolved |
| Wallet and net worth | the wallet contract excludes missing or forbidden balances from the sum. The reference-price projector values personal assets and included items from own active personal contracts with ESI average prices and adjusted-price fallback, adds own in-progress courier collateral as an explicit estimate, deduplicates accessible Corporation assets and wallet divisions by Corporation ID, records one point per day for up to 365 days and labels incomplete results as known components rather than a total | update permissions for every intended character, then compare representative personal contract, courier, wallet/order/escrow, personal-asset and accessible Corporation values with EVE. Verify multiple-day chart behavior; liquidation and actual courier cargo value remain unavailable |
| Reactions and minerals | negative reaction value creation is shown as a buy opportunity with a persistent group filter. Mineral fixtures prove latest-two-valid-day selection and percentage change; reduced-motion mode replaces automatic motion with manual horizontal scrolling | load a real reaction analysis, set and relaunch the filter, compare loss rows with the Reactions screen, then compare all eight regional mineral averages with current ESI history and inspect ticker motion/contrast |
| Automatic Contracts and Main-Hub status | the Dashboard compiles with one compact top-right status surface bound to the existing resumable Public Contracts progress and the shared exact Main-Hub snapshot. The deterministic policy test proves the 15-second startup boundary, six-hour minimum interval and overdue scheduling. Main-Hub automation is persisted and can be paused or resumed; an NPC Main-Hub change clears the former live snapshot | launch the app with an enabled partial Contract import and observe region/item progress; then let one scheduled Main-Hub scan advance through its pages, verify the selected Market Browser item immediately shows the same Hub snapshot, pause/relaunch/resume, and test the status box with keyboard and VoiceOver |
| Dashboard grouping and sizing | the former free-flowing adaptive grid is replaced by three stable semantic sections. Ready/Upcoming and Wallet/Net Worth are row-bound pairs with equal card height; each pair collapses together below its minimum width. The activity summary uses six cards in divisible three- or two-column rows and the complete dashboard has a readable maximum content width. A fresh native build and running-app inspection confirmed the normal-width hierarchy and aligned Finance row | resize the owner's normal working window from wide to narrow and confirm each pair changes as a unit, no localized value clips, the activity cards remain 3×2 or 2×3, and keyboard/VoiceOver order follows the visual section order |
| Build and tests | strict Swift formatting, localization validation and `git diff --check` pass. Complete isolated SwiftPM and unsigned native Xcode actions each build the app and pass 257 tests in 21 suites. The official OpenAPI document read on 2026-08-04 confirms `include_completed` covers the past 90 days, the six status values and the station/product/timing fields used here | launch the built macOS app and complete populated-data resizing, industry-filter, station/structure, keyboard, VoiceOver and German/English visual acceptance; automated success and schema inspection do not establish current private-ESI or EVE-client correctness |

## Net Worth, 2026-08-04

| Area | Automated evidence | Owner evidence still required |
| --- | --- | --- |
| Composition and unknown values | focused fixtures prove wallet plus priced assets plus remaining sell orders plus ESI-reported buy escrow, included items from own active personal item/auction contracts, own in-progress courier collateral and Corporation asset/wallet values counted once per Corporation. They reject missing scopes before transport, enforce Director/Accountant role boundaries, exclude requested contract items and BPCs, preserve failed contract-item pages as partial and decode legacy history with safe defaults | use **Update permissions** for every intended character, synchronize, then compare representative contract, courier, wallet/order/escrow, asset and Corporation values with EVE. Confirm BPC, unpriced-type, missing-role and unavailable-item-page states match the intended policy; ESI cannot reveal actual courier cargo value |
| History and interaction | the page builds with 7D/30D/90D/all range controls, combined/per-character chart modes, daily and manual same-day replacement, component deltas, a character table, snapshot selection and confirmed deletion. Running-app inspection confirmed normal-width German layout and explicit partial-state presentation without invoking manual capture or deletion | accumulate several real daily points, verify chart/delta behavior and deletion recovery expectations, then check narrow resizing, keyboard, VoiceOver and the English locale |
| Build and tests | isolated SwiftPM and unsigned native Xcode actions each build the app and pass 252 tests in 21 suites; strict formatting, localization parsing/duplicate checks, documentation suite links and `git diff --check` pass | live ESI behavior, browser reauthorization, signing/notarization, release performance and owner visual acceptance remain separate evidence |
| Scope, role and price diagnosis | stored authorization and source metadata were inspected read-only: all connected characters have the Net Worth scopes; one of two unique Corporations contributes values while the other reports missing Director and Accountant roles. The public reference-price endpoint currently contains a usable price for every locally held non-BPC type. Fixtures prove exact Corporation coverage, optional division decoding, named unpriced-type details and that location-name failures do not downgrade a complete valuation | after rebuilding, synchronize the Director character and create a new same-day snapshot. Confirm Corp cards say `1 of 2`, the division decoder warning is gone and current price warnings either clear or open to a named list; historical points remain unchanged |
| Updated build and tests | isolated SwiftPM and the unsigned native Xcode action each build the app and pass 256 tests in 21 suites. Focused Swift formatting, both localization catalogs, duplicate-key checks and `git diff --check` pass | populated German/English layout, keyboard, VoiceOver and EVE-client comparison remain open |

## Planner dual-hub sale scenarios, 2026-08-04

| Area | Automated evidence | Owner evidence still required |
| --- | --- | --- |
| Main/Home market and fees | the Planner context freezes separate exact-location order snapshots and per-location Sales Tax/Broker Fee inputs for Main and Home. A Home Player Structure uses the authenticated structure-market endpoint; unavailable Home access adds an explicit warning and leaves only the two Home scenarios unavailable | recalculate one real plan with the intended Main and Home Hubs, compare immediate and listed prices plus trader-specific fees at both locations, and confirm an inaccessible Home structure does not hide the Main result |
| Scenario logistics | deterministic fixtures prove Main-Hub purchases receive inbound Main-to-Home logistics, Main-Hub sales add a separate Home-to-Main finished-product route, Home-Hub sales add zero return cost, and exact identical Main/Home IDs remove all transport before a missing tariff can block calculation. Scenario total cost, profit, margin and ROI use the matching route rather than a shared total | compare one different-hub round trip and one identical-hub plan with the hauling provider and EVE; verify all four German cards, expanded calculation details, narrow-window layout, keyboard and VoiceOver behavior |
| Build and checks | isolated SwiftPM and unsigned native Xcode actions each build the app and pass 252 tests in 21 suites. Focused changed Swift files pass strict `swift-format`; English and German catalogs pass `plutil` and duplicate-key checks; suite links and `git diff --check` pass | running-app visual acceptance and current live market values remain open; automated fixtures do not establish live ESI or owner acceptance |

## Persistence recovery and public SDE operator configuration, 2026-08-05

| Area | Automated and migration evidence | Owner evidence still required |
| --- | --- | --- |
| SDE settings | source inspection confirms that the contact field and its save action are absent from the normal UI. The explicitly public project address `projekt-st@gmx.de` is compiled into the application, may be overridden by the local development environment and is normalized before ESI/SDE User-Agent construction; it is not an authentication credential. The update check and install actions remain | open Data & Settings → SDE in the populated app and confirm that no contact or email field is visible and that **Check for SDE update** still works |
| Store continuity | the legacy and historical stores were first consolidated into immutable backups. A temporary full-copy run proved V1-to-V2 migration and one-time merge before activation. The canonical production store then passed `PRAGMA quick_check` and contains 14 characters, 18 app settings, 152 current ESI metadata rows, 56 plans, one draft, four production rows and one SDE activation pointer. The former global `default.store` remains available as rollback evidence | confirm that all expected character cards and Profile market locations are visible after a normal launch; token prompts are a separate Keychain/signing check and do not indicate database loss |
| Defaults continuity | all 38 `@AppStorage` consumers use the stable application defaults store; startup copies missing app-owned keys from the old executable-name domain once. Both original preference files are preserved in the dated recovery directory | verify language, appearance, market/opportunity filters, disclosures and automatic-update choices on the owner's normal screens |
| Build and tests | isolated SwiftPM build passes and 260 tests in 21 suites pass; the unsigned native Xcode build succeeds. A second background-disabled open against the activated canonical store exits successfully without repeating recovery | signing/notarization, long-session behavior, live ESI/SDE responses, keyboard/VoiceOver and visual owner acceptance remain separate |

## Swift 6.1 GitHub Actions compatibility, 2026-08-05

| Area | Automated evidence | Evidence still required |
| --- | --- | --- |
| Failure boundary | GitHub Actions run `30987413552`, job `92245292586`, reached compilation on the `macos-15-arm64` runner with Swift 6.1.2 but did not start the test suites. Diagnostics identified stored static SwiftData schema metadata as non-Sendable and four large SwiftUI fingerprint expressions as exceeding that compiler's type-checking limit. The same revision had passed locally with Swift 6.3.3, so the evidence establishes a toolchain-compatibility failure rather than a failed domain assertion | the replacement commit must pass the same public GitHub Actions lane |
| Compatibility implementation | schema version identifiers and migration stages are computed static values with unchanged V1/V2 values and migration order. Asset, warehouse and market refresh fingerprints use explicit typed component arrays while preserving their fields, ordering and separators | no migration, live-service or owner acceptance is inferred from the source refactor |
| Local verification | `git diff --check` and strict `swift-format` pass for all changed Swift files. Isolated SwiftPM and unsigned native Xcode actions each build the application and pass all 260 tests in 21 suites | the replacement public GitHub Actions result remains separate until the same remote lane completes |
