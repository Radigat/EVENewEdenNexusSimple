# Acceptance matrix

Evidence captured through 2026-07-31:

- `swift test` with writable scratch/module caches: 87 tests
  in 14 suites passed.
- `swift format lint --recursive --parallel --strict Sources Tests` passed
  without findings. The current native Xcode project also completed a separate
  unsigned Debug build with `CODE_SIGNING_ALLOWED=NO`.
- A signed native Xcode build completed with the Profile navigation guard and
  shared SSO-session cache. The current local identity is ad-hoc
  `Sign to Run Locally`, so Data Protection Keychain entitlement behavior still
  requires an Apple Development signing identity for owner acceptance.
- `swift test --package-path Packages/EVEStaticDataKit`: 5 tests passed.
- `xcodebuild ... test CODE_SIGNING_ALLOWED=NO`: native app, framework,
  package and test targets built and the test action passed.
- The vendored package has the same file content as the
  `eve-static-data-kit` asset.
- Live SDE activation installed Tranquility build `3451778`; the activated
  catalog contains 48 categories, 1,610 groups, 52,822 types, 5,082
  blueprints, 19,137 activities, 36,499 materials and 6,329 products.
- A read-only production-recipe audit of that active catalog found 4,155
  unique published manufacturing/reaction products, zero duplicate published
  recipes, zero nonpositive outputs or times, and zero published recipes with
  unresolved materials. Tungsten Carbide resolves to the published formula
  `46207` with 10,000 output per run; the unpublished test formula is excluded.
- A read-only live facility query against that activated package resolved 14
  structure definitions, 10 compatible Standup service modules and 118
  compatible Manufacturing, Reaction, Invention, Copying, ME Research and TE
  Research rigs. The Research Lab exposes Copying, ME Research and TE Research;
  the Reprocessing Facility resolved 50% ore and 80% gas yield multipliers;
  Raitaru resolved as Medium with 1% ME and 15% TE.
- Public ESI system-detail acceptance resolved Jita/The Forge at
  `0.9459131360` as Highsec, Haine/Essence at `0.4364712536` as Lowsec, and
  B9E-H6/Immensea at `-0.3654290438` as Nullsec.
- The owner completed EVE SSO for six personal characters through the
  registered `http://localhost:52722/callback`.
- A post-fix live refresh recorded `fresh` for all six characters in all ten
  stored domains: skills, standings, blueprints, assets, industry jobs, open
  orders, order history, wallet balance, wallet journal and wallet
  transactions.

| Area | Automated technical evidence | Current EVE live evidence | Owner operating acceptance |
| --- | --- | --- | --- |
| SDE lifecycle and queries | pass: streamed staging, package-derived reaction rules, activation, backup, query and rollback fixtures; active pointers reject traversal/tampering, the confirmed preview must exactly match a fresh preview, and schema review is an explicit UI gate | pass: official build `3451778` downloaded, validated and atomically activated | pass: active build and catalog counts observed in the running app; pending re-check of the explicit schema-review control on the next schema-changing build |
| SSO and token lifecycle | pass: PKCE, exact callback binding, one-time/expiring state, replay and duplicate-query rejection, mixed RSA/EC JWKS, bounded token/JWT responses, local disconnect, and boundary tests; refresh tokens are Keychain-only; one shared SSO actor reuses access-token leases; generic authorization stores under the verified character ID and expected-character rejection remains covered at the core boundary; Data Protection Keychain has a secure legacy fallback | pass: six real character grants completed with the dedicated client registration and callback | pending re-check: use **Authorize or add character** for each character whose scopes need renewal and confirm that existing characters update while a new character is inserted; then verify the destructive local disconnect flow. EVE still needs one consent per character. First legacy access may request approval once per character; stable Apple Development signing is required to accept the prompt-free Data Protection path |
| ESI paging/cache/errors | pass: bounded response/page/path handling, inconsistent pagination rejection, non-retried generic POST, character-partitioned private validators, three-character system search, region/security detail chain, ACL-checked name search and used-location structure discovery from assets/jobs/orders | pass: ten private domains refreshed for six characters with ESI compatibility `2025-12-16`; all latest states are `fresh` | pending: reauthorize all characters, then owner-check **Find used structures** and name search in a known system |
| Industry system indices | pass: all current activity values are preserved and sorted while unknown future raw activities remain displayable | pass: public `/industry/systems` returned 5,485 systems; the Jita record contained manufacturing, invention, reaction, copying, ME research and TE research | pending visual confirmation beneath selected Profile systems |
| Blueprint BPO/BPC semantics | pass: endpoint quantity semantics remain separate from manual planning assumptions; top-level input accepts BPC consumed-copy cost or an owner-allocated BPO share, preserves the kind and treatment per line, reports missing costs, and includes entered costs in profit/ROI | pass: personal blueprint snapshots are `fresh` for all six characters; no market price is inferred for a blueprint instance | pending owner check of BPC and BPO entries against an actual plan; automatic BPC valuation and a default BPO amortization policy remain intentionally absent |
| Combined assets, targets and reservations | pass: current `is_singleton` schema, bounded nested inventory resolution, multi-owner station grouping, cycle exclusion, target protection and reservation conflict; SwiftPM and unsigned native Xcode actions pass with 87 tests in 14 suites | underlying personal asset snapshots were `fresh` for all six characters on 2026-07-30; the new complete per-character persisted payload and public station-name lookup were not refreshed live in this implementation turn | pending: run **Sync all** once, open **Assets & Warehouse**, expand every character under a shared station, set one present and one absent target, and compare actual/free/missing values with EVE; player structures without an accepted Profile name may remain ID-labelled |
| Personal wallet overview | pass: focused tests cover required scope, character endpoint, authenticated request, available-value aggregation, missing values and stale portfolio state; SwiftPM and native Xcode test actions pass | underlying wallet-balance snapshots were `fresh` for all six characters at ESI compatibility `2025-12-16`; the new dedicated refresh command was not rerun against live ESI | pending: open Wallet, refresh all, compare total with each character row and check partial/forbidden messaging |
| Jita depth pricing and trader fees | pass: depth, insufficient volume and fee scenarios; Accounting, Broker Relations, unmodified Jita-owner standings, the 1% NPC floor and unavailable source states are deterministic and source-bound | the owner character capability snapshots were previously `fresh`; the new derived fee display was not refreshed during this implementation turn | pending: choose the trader in Profile, press **Refresh Trader Fees from ESI**, and compare both displayed rates with the in-game Jita IV-4 order window |
| Industry graph and costs | pass: recursion, Want-to-runs conversion, job-wide ME/TE rounding, multiplicative facility modifiers and incomplete-cost blocking; entered BPC/BPO costs are separate; installation is split into system index, Facility Tax, SCC and Alpha; immediate/listed sales retain gross, Sales Tax, Broker Fee and net revenue separately | pass: active build recipe audit selects only unique published, fully resolved manufacturing/reaction definitions | pending owner comparison of one complete calculation against in-game job and market windows |
| Logistics costing | pass: SDE packaged-volume lookup, separate inbound/outbound routes, complete Jita replacement collateral, deterministic whole-item first-fit-decreasing allocation, automatic 383,000 m³ split into 350,000 and 33,000 m³ contracts, maximum of volume charge versus 0.5% collateral, per-contract 1 million ISK upward rounding, inclusion in profit/ROI and blocking only when one indivisible item exceeds the configured limit; 350,000 m³ remains the default | active SDE build `3451778` is the configured volume source; Ark resolves to 18,500,000 m³ and as one indivisible item still requires a provider-confirmed exception; no live provider calculator value was available in the supplied screenshot | pending: enter the provider's current ISK/m³ rate and compare the generated contracts with its calculator; confirm route labels and operational restrictions |
| Planner continuity, warehouse gap, Multibuy export and Production Overview | pass: successful plans persist as the active immutable snapshot; draft production/manual-stock input restores after navigation; the newest saved Production Basis and installation-cost references load before the first calculation is enabled; combined factual stock minus protected targets and active reservations feeds manufacturing/reaction allocation; each material row shows warehouse, target, used, still needed, produce and buy; material rows retain SDE category/group and production activity, show only true non-producible inputs in source-grouped raw-material sections, and place producible intermediates below; the deterministic EVE Multibuy clipboard export includes only positive `toBuy` quantities as plain item-name/quantity lines; **Record production** creates one row per requested item with the 21 columns of the `EVE-indu- Delve.xlsx` `Produktionsübersicht`; formulas cover production cost, total sale, unit cost, 10% minimum price, projected profit, margin and real profit; the Planner's entered line-level BPC/BPO cost is copied into the corresponding row, while BPO/BPC cost, sale price per unit and sold units remain editable; an unentered BPO/BPC cost and other unresolved costs remain unavailable instead of becoming zero; the newest five rows appear below Planner input and the full overview is a sidebar destination | spreadsheet reference inspected 2026-07-30; recorded rows retain the plan's frozen SDE, ESI, price and rule provenance and need no additional live endpoint; the new warehouse allocation was not exercised against current live assets | pending owner check: after **Sync all**, calculate a plan with combined warehouse enabled and verify one target is retained; confirm the raw-material/intermediate grouping and installation costs; copy the shopping list and import it into EVE Multibuy; then visit Characters and Wallet, return to Planner, record a multi-item plan and compare the five-row preview with the full overview, including the carried BPC/BPO value and later edits to BPO/BPC cost, sale price and sold units |
| Production basis and facility selection | pass: focused tests cover ME-before-TE, region/security resolution and inheritance, legacy profile review, SDE structure/service/Science-rig dogma, service compatibility, size-compatible rigs, slot caps, manual assignment, automatic reaction-refinery selection, and system-bound Invention/Copying/ME/TE Research selection | live ESI verified one Highsec, Lowsec and Nullsec system; read-only live SDE build `3451778` resolved 14 structures, 10 service modules and 118 Manufacturing/Reaction/Science rigs | pending visual re-check in the running Profile page, including service-module pickers, Reprocessing yields, automatic Tatara/Athanor and all four Blueprint-activity choices; facility tax remains manual, fitted/online service state cannot be inferred from ESI and Thukker rigs remain `needsReview` |
| Invention skill matrix | pass: every connected character remains a column; missing snapshots stay unknown; fresh missing skills are zero; deterministic broad-readiness ranking | active SDE contains 45 published Science skills | pending owner review of all character columns and full-row disclosure control; exact blueprint-specific invention chance remains outside v1 |
| SwiftUI accessibility | app target builds; labels and identifiers, including the exclusive character-scope disclosure, Wallet total/rows/refresh controls, are present; dirty Profile navigation offers save, discard and cancel commands | not applicable | pending owner check of character scope expansion, Profile warning plus manual VoiceOver and keyboard pass |
| Ravworks/Excel comparison | the 21 main columns and their derived formulas are represented by `StoredProductionOverviewRow` plus `ProductionOverviewCalculation`; focused tests cover the spreadsheet equations and unknown-cost propagation | `EVE-indu- Delve.xlsx`, worksheet `Produktionsübersicht`, inspected 2026-07-30; no live EVE call required | pending visual and value comparison in the running app |

Passing technical tests does not imply a live-service or owner acceptance.
The rows above mark those layers independently; no token, authorization code,
character identifier or private ESI payload is retained as acceptance evidence.
