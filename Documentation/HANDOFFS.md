# Skill handoffs

Every row is a handoff envelope. “Version” is the contract or rule version, not
a claim that a live service was exercised.

| Source skill | Source identity and version | Accepted output and target boundary | Open risk | Next skill |
| --- | --- | --- | --- | --- |
| `eve-documentation-kit` | ADR-001; app contract v1; live evidence 2026-07-30 | ownership map, source-state rules and acceptance separation | planner and accessibility owner acceptance remain open | `eve-sde-integration` |
| `eve-sde-integration` | CCP Tranquility build `3451778` | Level-4 profile for seven JSONL datasets; app owns activation persistence; target adapter derives structures, compatible service modules and rigs plus packaged-volume logistics facts from published types and dogma | future builds still require the preview and schema gate | `eve-static-data-kit` |
| `eve-static-data-kit` | package identity from vendored skill asset | unchanged package in `Packages/EVEStaticDataKit`; streamed catalog batches and package-derived reaction rule profile | live schema drift remains an update-time gate | `eve-auth-kit` |
| `eve-auth-kit` | EVE SSO v2; callback `http://localhost:52722/callback`; security revision 2026-07-30 | exact one-time loopback callback binding, stored `AuthorizationSnapshot`s and short-lived `AccessTokenLease`s; one reused SSO actor per client ID; the verified SSO character ID automatically reauthorizes its matching local record or adds a new one; Data Protection Keychain with legacy migration/fallback and an explicit local disconnect command; refresh tokens never enter app persistence | prompt-free Data Protection access needs a stable Apple Development identity; EVE requires one browser consent per character during scope renewal; local disconnect does not remotely revoke website access | `eve-esi-kit` |
| `eve-esi-kit` | ESI compatibility `2025-12-16`; current OpenAPI reviewed 2026-07-30 | bounded response/pagination handling, non-retried generic POST, public system/constellation/region details, name-based ACL structure search, known-location discovery from assets/jobs/orders, ACL-checked structure detail and scope-checked personal wallet balance; conditional private caches are character-partitioned | ESI cannot enumerate every docking ACL or expose facility tax; structures never used by a character still require name search; a central error-budget scheduler remains open | `eve-character-kit` |
| `eve-character-kit` | per-character ESI snapshot | identity, skills and unmodified standings with explicit source states; optional capability and personal wallet-balance snapshots persist for cross-character presentation and trader-fee calculation | clone state remains unknown unless explicitly configured | `eve-assets-kit` |
| `eve-assets-kit` | immutable personal asset snapshots; warehouse projection v1; live source evidence 2026-07-30 | sourced snapshots persist per character; bounded container roots group location → owner → contents; combined factual quantities retain snapshot IDs and exclude cycles/unresolved roots; reservations and targets remain outside factual Assets | the owner must run **Sync all** once to populate the new persisted payload; inaccessible player-structure names remain explicit IDs unless already accepted in Profile | `eve-industry-kit` |
| `eve-blueprints-kit` | SDE definition plus ESI owned instance | BPO/BPC, runs, ME and TE remain endpoint-derived; Profile exposes separate Invention, Copying, ME Research and TE Research facility selections without inventing an owned-blueprint job | concrete invention, copying and research job planning remains unsupported | `eve-market-kit` |
| `eve-market-kit` | The Forge `10000002`, Jita `30000142`, station `60003760`; fee rule `ccp-market-fees-2026-07-02`; private orders live 2026-07-30 | open and historical personal-order snapshots are `fresh` for six characters; order-depth quotes retain separate provenance; one connected trader supplies sourced market fees; complete Jita sell-depth quotes supply logistics collateral without moving haulage economics into Market | public Jita order-book freshness and owner comparison of the displayed in-game fee remain open | `eve-reactions-kit` |
| `eve-reactions-kit` | SDE reaction activity plus profile rule v1 | reaction candidates without ME/TE; automatic selection ranks refineries with an explicitly configured Reactor service only within the configured reaction system | ESI cannot confirm the live online service-module state | `eve-industry-kit` |
| `eve-assets-kit` → `eve-industry-kit` | combined warehouse policy v1; source snapshots remain immutable | target stock is an Industry-owned protection policy; only factual quantity above target enters allocation, then active reservations are subtracted; manufacturing and reactions share the projection without implying transport | concrete Science-job planning, cross-location hauling decisions and reservation reconciliation after manual in-game moves remain open | `eve-ui-kit` |
| `eve-blueprints-kit` → `eve-industry-kit` | rules `2026.07-v1`, `ccp-market-fees-2026-07-02` and owner reference `standard-haulage-split-2026-07-30`; SDE facility-reference build `3451778`; spreadsheet reference inspected 2026-07-30 | preserves manual BPC versus BPO provenance and explicit line-level cost treatment; derives runs and facility effects; splits oversized routes into whole-item contracts; composes SDE volume and allocated Jita collateral into separately rounded contracts; freezes blueprint, system index, Facility Tax, SCC, Alpha and logistics costs; derives gross, Sales Tax, Broker Fee and net revenue per sale scenario | automated BPC market valuation, full-BPO amortization policy, concrete Science-job planning, Reprocessing job planning, Thukker special modifiers and ship-class refinement remain open | `eve-ui-kit` |
| `eve-ui-kit` | Assets & Warehouse and expanded Planner view states, 2026-07-31 | location-first panels expose owner disclosure groups and contents; target editor exposes actual, target, free and missing quantities; Planner shows warehouse, target, used, still needed, produce and buy columns | manual accessibility, large-inventory performance and owner workflow acceptance remain pending | `eve-testing-kit` |
| `eve-testing-kit` | frozen synthetic fixtures and native target test, 2026-07-31 | 87 deterministic tests in 14 suites cover existing contracts plus multi-owner station grouping, nested contents, cycle exclusion and target protection; SwiftPM and unsigned native Xcode test actions pass | live all-character warehouse population and owner visual comparison remain open | `eve-update-kit` |
| `eve-update-kit` | update workflow v1; security revision 2026-07-30 | check, exact-preview confirmation, explicit schema acknowledgment, backup, staging, validation, activation and rollback coordination | destructive replacement is never automatic; disk-space preflight and retention policy remain open | `eve-documentation-kit` |

Partial, forbidden, stale and unresolved inputs generate warnings or block only
the affected result. They never silently become empty or zero-valued data.
