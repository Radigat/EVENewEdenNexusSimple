# Skill handoffs

Every row is a handoff envelope. “Version” is the contract or rule version, not
a claim that a live service was exercised.

| Source skill | Source identity and version | Accepted output and target boundary | Open risk | Next skill |
| --- | --- | --- | --- | --- |
| `eve-documentation-kit` | ADR-001; app contract v1; live evidence 2026-07-30 | ownership map, source-state rules and acceptance separation | planner and accessibility owner acceptance remain open | `eve-sde-integration` |
| `eve-sde-integration` | CCP Tranquility build `3451778`; active local build `3453885` inspected 2026-08-01; schema changelog checked 2026-07-31 | Level-4 profile for seven JSONL datasets; app owns activation persistence; target adapter derives structures, compatible service modules and rigs plus packaged-volume logistics facts from published types and dogma; the Moon purchase analysis consumes exactly the complete published `Moon Materials` group; update evidence separates active build, official build and latest schema boundary | future schema-changing builds still require the preview and explicit schema gate | `eve-static-data-kit` |
| `eve-static-data-kit` | package identity from vendored skill asset | unchanged package in `Packages/EVEStaticDataKit`; streamed catalog batches and package-derived reaction rule profile | live schema drift remains an update-time gate | `eve-auth-kit` |
| `eve-auth-kit` | EVE SSO v2; callback `http://localhost:52722/callback`; Corporation-asset scopes added 2026-08-02 | exact one-time loopback callback binding, stored `AuthorizationSnapshot`s and short-lived `AccessTokenLease`s; requested scopes now include Corporation Assets, Corporation Roles and Corporation Divisions in addition to the existing personal and Player Structure scopes; refresh tokens remain Keychain-only | existing grants need browser consent once before Corporation hangars can be synchronized; prompt-free Data Protection access needs a stable Apple Development identity; local disconnect does not remotely revoke website access | `eve-esi-kit` |
| `eve-esi-kit` | ESI compatibility `2025-12-16`; current Corporation Assets/roles/divisions OpenAPI reviewed 2026-08-02 | existing bounded transport plus Director preflight, paginated `/corporations/{corporation_id}/assets`, Corporation identity and division-name retrieval; missing scope, role and endpoint access map to explicit source states | the Corporation Assets endpoint requires `Director`; division names can be partial; no live private Corporation payload has been requested for acceptance | `eve-character-kit` |
| `eve-esi-kit` → `eve-ui-kit` | public-contract index and opt-in automation contract revised 2026-08-04; current OpenAPI requiredness and one bounded response verified without a bulk import | all current ESI regions are scheduled sequentially; optional `for_corporation` remains unknown when omitted and the legacy store migrates transactionally without discarding contracts or items. Region and item endpoints follow all reported pages, require page consistency before activation, persist cache eligibility and item completion, resume locally, expose partial/throttled state, and join active-SDE metadata for local search; no authorization is used. Industry obtains one bounded exact-type offer snapshot plus its atomic coverage state, so a partial, stale or failed index cannot become false Blueprint absence. Cancelled local searches recheck cancellation before publishing their result. First-import completion remains independent from current freshness, failed never-successful regions remain scheduled at their retry time, and three schema-decoding failures stop one run before the same defect repeats across all regions. One manual start persists automatic continuation, app launch waits at least 15 seconds, complete work refreshes no sooner than six hours, and manual stop persists the disabled state | a complete live all-region import, production-database migration, long-session RSS and running-app owner acceptance remain open; Player Structure names need a separately authorized resolver; future CCP rate policy cannot be guaranteed and live response headers remain authoritative | `eve-testing-kit` |
| `eve-character-kit` | per-character ESI snapshot | identity, skills and unmodified standings with explicit source states; optional capability and personal wallet-balance snapshots persist for cross-character presentation and trader-fee calculation | clone state remains unknown unless explicitly configured | `eve-assets-kit` |
| `eve-assets-kit` | immutable personal plus opt-in Corporation asset snapshots; warehouse projection v5; Corporation contract 2026-08-02 | one shared switch controls Corporation inclusion in All items, production Warehouse and Planner; Corporation is a separate owner kind, `CorpSAG1...7` keep raw provenance and resolved division labels, and snapshots from multiple Directors of the same Corporation are deduplicated before quantity projection; exact-location and ship-descendant exclusions remain unchanged | owner must reauthorize a Director and synchronize; live private Corporation data, running-app layout and long-session RSS remain owner acceptance | `eve-industry-kit` |
| `eve-blueprints-kit` | SDE definition plus sourced ESI owned-instance portfolio; research rule `ccp-blueprint-research-2026-07-v1` | BPO/BPC, owner, location, runs, ME and TE remain endpoint-derived; the target persists complete personal snapshots and joins a selected BPO to its SDE manufacturing/research definition; official level multipliers produce step and cumulative ME/TE quote inputs without creating an executable job | current replacement cost is not historical spend or contract-market value; extra SDE research-material scaling remains blocking until separately verified; concrete invention, copying and research job installation remains unsupported | `eve-market-kit` |
| `eve-market-kit` | ESI compatibility `2025-12-16`; current official market caching/rate-limit guidance reviewed 2026-08-04; NPC hub region/station IDs; fee rule `ccp-market-fees-2026-07-02`; Planner dual-hub sale contract revised 2026-08-04 | Planner requests separate location-bound order snapshots and per-location trader fees for the Market Settings Main and Home Hubs; a failed or inaccessible Home Player Structure leaves only Home sale scenarios unavailable. Bounded per-type regional responses discard non-target stations before retention. Main, Home and Coalition persist as the only market roles, while unassigned saved locations are candidates rather than markets; each role location owns its trader and fee evidence; Moon analysis consumes the deduplicated selected role set, uses sell-only per-type public requests for NPC hubs, reuses an unchanged snapshot within five minutes, and uses one paginated authenticated `/markets/structures/{structure_id}/` request per selected Player Structure with local type filtering; the cross-region Market Browser remains an independent public all-region comparison | selected Player Structures still require `esi-markets.structure_markets.v1`; Player Structure ACLs and owner-defined broker fees can remain unavailable; live structure-order, long-session RSS and visual owner acceptance remain open | `eve-reactions-kit` |
| `eve-reactions-kit` | active SDE build plus reaction rule `2026.07-v1`; run-limit clarification 2026-08-01; procurement logistics `main-hub-to-production-2026-08-01` | every complete published reaction formula is analyzed for persisted runs (default 100), input cost plus inbound logistics, output replacement value plus direct-purchase logistics, complete make-or-buy savings, immediate-sale spread, value status and margin; both logistics paths use the shared SDE-volume, replacement-collateral, contract-splitting and rounding routine; the one-job run maximum is derived from the 30-day window, SDE duration and accepted time multiplier, with a minimum of one run and an explicit required-job count | live ESI prices are time-bound; missing depth, volume, collateral or an incomplete enabled tariff remains unavailable; immediate-sale outbound transport is outside the comparison; ESI cannot confirm the live online reactor service and owner comparison with the in-game reaction window remains open | `eve-industry-kit` |
| `eve-assets-kit` → `eve-industry-kit` | combined warehouse policy v2; source snapshots remain immutable | target stock is an Industry-owned protection policy; only factual quantity above target enters availability, then active reservations are subtracted; Industry consumes it only for a raw material or intermediate explicitly set to warehouse; any shortfall remains a purchase; actual stock consumption receives an informational countervalue from the same full-requirement weighted Main Hub unit price without mutating the source snapshot or adding the value twice | concrete Science-job planning, historical stock acquisition cost and reservation reconciliation after manual in-game moves remain open | `eve-ui-kit` |
| `eve-blueprints-kit` → `eve-industry-kit` | rules `2026.07-v1`, `ccp-market-fees-2026-07-02` and logistics `planner-hub-routes-2026-08-04`; SDE facility-reference build `3451778`; spreadsheet reference inspected 2026-07-30 | preserves manual BPC versus BPO provenance and explicit line-level cost treatment; derives runs and facility effects; every producible component and reaction intermediate receives a full-quantity make-or-buy analysis with Main Hub depth, batch-rounded direct inputs, installation and inbound logistics; applied preferences control the actual graph. Planner freezes separate Main/Home immediate and listed sale scenarios: Main adds Home-to-Main finished-product logistics, Home adds no return route, and exact identical locations remove both routes before tariff validation. Each resulting `IndustryJobCost` freezes product, runs, output quantity, ME/TE, top-level status, facility and installation cost for the build list; all purchases use the Profile Main Hub | automated BPC market valuation, full-BPO amortization policy, concrete Science-job planning, Reprocessing job planning, Thukker special modifiers and ship-class refinement remain open; live dual-hub and private-structure comparison plus visual owner acceptance remain open | `eve-ui-kit` |
| `eve-ui-kit` | shared disclosure, warehouse, Planner summary, Moon comparison, Market Browser and production-book interaction revision 2026-08-03 | The Market Browser navigation entry provides three-character active-SDE item search, best and 5%-average price metrics, sell/buy volume, gross average margin, explicit partial-source and structure-authorization status, sortable full-width Sellers and Buyers tables, and region/location/price/quantity/location-kind plus Highsec/Lowsec/Nullsec/unknown filters. Each location row places the EVE-rounded security value before the station or structure name and colors that value with CCP's eleven-level security palette; the separately resolved system name is intentionally omitted from this cell because the EVE location name already carries that context. The visible security number remains a non-color signal, and location sorting still uses the location name rather than the prefixed number. Buyer rows include range and minimum volume; seller and buyer rows retain expiry and regional ESI update time. Route controls and jump counts are intentionally absent. Existing disclosure, Warehouse, Planner, Moon comparison and production-book contracts remain unchanged | running-app resizing, keyboard/VoiceOver, long all-region load, player-structure wording, security-color contrast and comparison with current in-game orders remain owner acceptance; historical completed-sale volume is not yet presented | `eve-testing-kit` |
| `eve-ui-kit` → `eve-testing-kit` | Public Contracts UI contract revised 2026-08-05 | navigation exposes manual start/continue and stop, persistent progress, local three-character item search during synchronization, active-SDE category/group facets, offered/requested/both selection and active public-contract results; `Ark` can be narrowed to `Ship` and `Jump Freighter` without hiding incomplete synchronization state. The collapsed surface always shows a critical region-error warning, while the disclosure separates first-import completion, current freshness, failures and earliest retry. Until initial region and detail work is complete, a separate non-collapsible bold red warning states the current two- to three-day expectation, background/relaunch continuation, reduced usability and ongoing optimization. Result cards omit the Contract ID, show Region → Location → category/group → type → expiry → amount, and use two columns above the compact-width threshold or one below it | actual long-run duration, CPU/RSS/bandwidth, keyboard/VoiceOver behavior, light/dark appearance, resizing and comparison with the in-game contract search remain owner acceptance | `eve-testing-kit` |
| `eve-testing-kit` | frozen synthetic fixtures and native target test, 2026-08-02 | Five focused Market Browser tests prove partial region retention, field and provenance mapping, location/system/security resolution, structure-name enrichment, security and location filtering, EVE-client display rounding including the small-positive exception, exact best-volume 5% weighting, outlier exclusion, active volumes and gross average-margin semantics; full-suite counts are recorded in `Documentation/ACCEPTANCE.md` | live all-region ESI compatibility, connected-character structure-name resolution, long-running rate-limit/cache behavior, running-app layout and historical market-volume coverage remain open | `eve-update-kit` |
| `eve-testing-kit` → `eve-update-kit` | program-wide deterministic suite revised 2026-08-04 | dual-hub Planner regressions prove separate Main/Home price and fee inputs, inbound plus Main-return logistics, zero Home return cost and exact identical-location short-circuiting. Seventeen Public Contracts tests retain exact-type bounded lookup and complete-coverage semantics; a market regression proves non-target stations are discarded before retention. Complete SwiftPM and unsigned native Xcode actions each build the app and pass 252 tests in 21 suites; the nested static-data package passes 5 tests; both localization catalogs validate without duplicate keys | no complete live all-region import, production-database migration, long-duration CPU/RSS or rate-limit session, private-ESI refresh or visual owner acceptance is claimed | `eve-update-kit` |
| `eve-update-kit` | update workflow v1; security revision 2026-07-30; live metadata check 2026-07-31 | check, exact-preview confirmation, explicit schema acknowledgment, backup, staging, validation, activation and rollback coordination; candidates move only to a newer official build and never downgrade an active catalog | destructive replacement is never automatic; disk-space preflight and retention policy remain open | `eve-documentation-kit` |

Partial, forbidden, stale and unresolved inputs generate warnings or block only
the affected result. They never silently become empty or zero-valued data.

## Market Settings revision 2026-08-02

| Source skill | Source identity and version | Accepted output and target boundary | Open risk | Next skill |
| --- | --- | --- | --- | --- |
| `eve-market-kit` | central market-role contract 2026-08-02 | persisted Main, Home, Coalition and Trade roles over exact NPC stations or Player Structures; Main accepts added resolved NPC stations, Coalition accepts only resolved structures; Moon consumes all configured locations and Reactions one selected location | live structure ACL and price coverage remain time-bound and owner-specific | `eve-ui-kit` |
| `eve-esi-kit` → `eve-ui-kit` | public-contract manual-start isolation 2026-08-02 | manual Public Contracts refresh bypasses only the local automatic safety date; public contract endpoints remain independent of SSO and Corporation Asset scopes while ESI transport limits still apply | full live all-region duration and rate-limit session remain owner acceptance | `eve-testing-kit` |
| `eve-testing-kit` | deterministic market-role and automation fixtures 2026-08-02 | custom NPC Main, identical Main/Home, Coalition structure persistence, invalid Coalition rejection and manual contract-refresh policy are covered | running-app role editing, live private structure markets and visual comparison remain pending | `eve-update-kit` |

## Moon analysis image export 2026-08-02

| Source skill | Source identity and version | Accepted output and target boundary | Open risk | Next skill |
| --- | --- | --- | --- | --- |
| `eve-ui-kit` | Moon comparison PNG export contract 2026-08-02 | the UI renders the current immutable analysis snapshot into a complete non-scrolling PNG with all configured hubs, all Moon materials, explicit unavailable states, rank colors and a localized export timestamp; the save panel only chooses the destination | extreme custom hub counts can create a very wide image; running-app legibility and save-panel behavior remain owner acceptance | `eve-testing-kit` |
| `eve-testing-kit` | isolated SwiftPM and unsigned native Xcode regressions, 192 tests in 20 suites, 2026-08-02 | both actions build the app and cover the snapshot and ranking inputs consumed by the exporter; no live ESI request is required for image creation | there is no automated pixel-level or save-panel UI test, so full-size image inspection remains manual | `eve-update-kit` |

## Main Hub manufacturing opportunities 2026-08-02

| Source skill | Source identity and version | Accepted output and target boundary | Open risk | Next skill |
| --- | --- | --- | --- | --- |
| `eve-market-kit` → `eve-industry-kit` | persisted exact-hub surviving-order-depletion ledger v1; versioned last-known-good opportunity snapshot v1; ESI compatibility `2025-12-16`; active SDE manufacturing definitions | an opt-in, cancellable catalog scan reads each reported regional order page once, retains only the exact configured NPC Main Hub, and locally evaluates complete published manufacturing products with at least one active buy or sell order there; the first scan is a demand baseline, and later same-UTC-day snapshots add only volume decreases for the same surviving order ID/type/side/location; disappeared or new orders are ignored and missing demand remains unavailable, never zero; each successful complete result is atomically stored and restored before an optional refresh | this is a conservative observed lower bound rather than complete transaction history; large live regions can require a long paginated read; Player Structure Main Hubs remain open; unreadable saved opportunity data is reported and protected from silent replacement | `eve-ui-kit` |
| `eve-blueprints-kit` + `eve-assets-kit` → `eve-industry-kit` | selected-character Blueprint snapshot, optional Corporation asset snapshot, combined exact-assignment Warehouse scope, protected stock targets and active-plan reservations | personal, Corporation, not-owned and unknown filters retain evidence state; Warehouse allocation merges exact facilities selected for manufacturing categories, reactions, invention, copying and research, then uses only factual eligible stock after target and reservation protection; configured but unused structures and Main-Hub market inventory are excluded | Corporation BPO/BPC kind is not inferred from asset type alone; incomplete ownership, unresolved facility IDs or Warehouse evidence remains unknown and cannot support a not-owned claim or zero-stock Shopping List | `eve-ui-kit` |
| `eve-industry-kit` → `eve-ui-kit` | opportunity calculation contract v3 plus recursive production-tree projection; industry rules `2026.07-v1`; market-fee rule `ccp-market-fees-2026-07-02`; round-trip logistics extension 2026-08-03 | target quantity derives whole runs and output; ME, TE, the selected per-category facility multipliers, system index, material depth, installation, Main-Hub gross revenue and configurable trader fees feed scenario cost, profit, ROI, ISK/h and ISK/m3. An on-demand complete reference graph retains every producible intermediate while a separate selected graph applies Build/Buy choices and stock-first shortfall handling. Inbound purchases are grouped by consuming facility and top-level outputs return to the Main Hub; incomplete pricing remains unavailable | no explicit intermediate transfer edge exists yet between different reaction/manufacturing facilities; copy, invention, research and bundled-contract costs are not attributable and remain outside production cost | `eve-testing-kit` |
| `eve-blueprints-kit` + `eve-assets-kit` + `eve-market-kit` → `eve-ui-kit` | sourced personal Blueprint portfolio, exact production/science Warehouse scope and local public-contract index, 2026-08-03 | every producible tree node exposes SDE Blueprint type and exact-location BPC/BPO availability; copying and invention are recommended only when a usable owned source exists at the configured activity location. ME/TE research-location BPC/BPO counts are reported separately and are not mistaken for immediately usable production stock. Corporation asset-only Blueprint presence remains unknown kind. Indexed Blueprint offers show offer count and lowest whole-contract price without treating bundle price as per-run cost | Corporation Blueprint endpoint data is not implemented; contract bundle composition, copy/invention/research job cost and Blueprint choice persistence beyond the open tree remain open | `eve-testing-kit` |
| `eve-ui-kit` | supplied EVE Guru Industry Calculator image and linked video reference; recursive-tree revision 2026-08-03 | the dedicated navigation entry provides the three-tab filter deck, persisted favorites and Cost Sheet, session-local ownership/Blueprint/product filters that start fully enabled, a dense top-anchored scenario grid, safe stop/progress, and an always-visible lower Warehouse/Main-Hub/Shopping workbench with Multibuy copy. Candidate name and chevron open the recursive tree; its quantity can be changed without rescanning the complete candidate list, intermediate rows expand independently, Build/Buy is selectable per type, stock coverage is green/yellow/red and Blueprint source status is selectable with explicit cost limitations | the current exact build still needs a populated saved-list relaunch and final owner comparison of tree resizing, deep recursion, demand movement and multi-location Warehouse/Blueprint contents | `eve-testing-kit` |

## Shared EVE entity presentation 2026-08-03

| Source skill | Source identity and version | Accepted output and target boundary | Open risk | Next skill |
| --- | --- | --- | --- | --- |
| `eve-ui-kit` | shared EVE-name presentation contract v1, 2026-08-03 | `EVEEntityText` gives displayed item, reaction, region, system, station, structure, market-location and blueprint names one larger semantic style, a locally configurable shared color in Data & Settings, a direct clipboard action and localized success/failure feedback. `EVEEntityLabel` supplies the same style where an enclosing disclosure already owns the row click. Search results and picker entries retain their selection action; the resulting visible selection is copyable. Domain snapshots, ESI/SDE provenance and unavailable states are unchanged | the running app still needs owner confirmation of color contrast, compact-table sizing, popover placement, keyboard and VoiceOver behavior across representative screens | `eve-testing-kit` |

## Dashboard 2026-08-03

| Source skill | Source identity and version | Accepted output and target boundary | Open risk | Next skill |
| --- | --- | --- | --- | --- |
| `eve-character-kit` → `eve-industry-kit` | sourced character skills and character-industry jobs; ESI compatibility `2025-12-16` | the last successful or retained-stale job list is persisted separately per character without a SwiftData schema migration; activity IDs remain distinct for Manufacturing, Reactions, Copying, Invention, ME Research and TE Research. Manufacturing, Reactions and the one shared Science pool derive capacity only from available skill evidence; missing skill sources produce an unknown or partial denominator rather than zero | existing characters need one synchronization before their job list is available; live slot totals and ready-state timing still require comparison with the EVE client | `eve-ui-kit` |
| `eve-esi-kit` → `eve-industry-kit` → `eve-ui-kit` | official character-industry OpenAPI inspected 2026-08-04; app compatibility `2025-12-16` | current optional job fields and legacy persisted snapshots decode together. Each synchronized job retains its resolved NPC station or authorized Player Structure name when available. The linked Dashboard/detail projection treats an elapsed cached active job as ready while keeping paused jobs paused, exposes status and activity filters, and never drops a job because its facility name is unresolved | `include_completed` is limited by ESI to 90 days. Player Structure ACL, missing detail scope, stale snapshots and inaccessible facilities remain partial/unresolved; populated live filtering, timing and facility comparison are owner acceptance | `eve-testing-kit` |
| `eve-market-kit` → `eve-ui-kit` | regional ESI history plus reference-price policy `esi-average-then-adjusted-v1` | the Mineral ticker shows the latest regional daily average and change against the preceding available market day for eight minerals. Personal-asset plus wallet valuation records one source-bound snapshot per day, retains at most 365 days and shows total and per-character trends; unpriced asset types remain explicit and incomplete totals are labeled as known components | regional history is not an intraday quote; ESI average/adjusted reference prices are an explicit valuation policy rather than guaranteed liquidation value; Corporation assets are excluded from character net worth | `eve-testing-kit` |
| `eve-reactions-kit` → `eve-ui-kit` | current immutable reaction-analysis snapshot | only negative value-creation rows are labeled as buy opportunities; the selected reaction-group filter is stored across relaunches. Missing analysis remains unavailable and is started only through the existing explicit analysis command | the analysis result itself remains session-bound and live prices are time-bound | `eve-testing-kit` |
| `eve-ui-kit` → `eve-testing-kit` | dashboard presentation contract v2 | Dashboard is the first navigation item and startup destination. Instead of one free-flowing adaptive card stream, it uses stable Industry & Jobs, Finances, and Market & Opportunities sections. Ready/Upcoming and Wallet/Net Worth remain fixed pairs that switch together between two columns and one column; paired cards share their row height, the six activity cards use divisible three- or two-column layouts, and content is capped at a readable maximum width. German and English strings are included | running-app inspection confirmed the normal-width grouping and aligned Finance row; final owner acceptance still covers narrow resizing, animation, keyboard, VoiceOver, real-character synchronization and value comparison | `eve-testing-kit` |
| `eve-testing-kit` | synthetic dashboard and industry-job projection fixtures, 2026-08-04 | SwiftPM and unsigned native Xcode actions each build the app and pass 257 tests in 21 suites; dashboard tests cover activity mapping, shared-slot capacity, unavailable skills, running versus ready jobs, elapsed cached jobs, paused jobs, current/legacy job decoding, consecutive market-day change and exclusion of unpriced assets from known components | no private ESI payload, running-app visual inspection or EVE-client comparison was performed | `eve-update-kit` |
| `eve-market-kit` → `eve-ui-kit` | exact configured NPC Main-Hub projection plus policy `six-hour-startup-delayed-explicit-consent-v2` | after Planner configuration is available, an explicitly enabled schedule starts one exact Main-Hub refresh no sooner than 15 seconds after startup and no more often than every six hours. The first launch under consent v1 pauses the former implicit setting. Each catalog scan follows every regional page for correct demand comparison, atomically persists the order-ID ledger, then releases the decoded ledger and bulk ESI response bodies. Only a compact per-type best-buy/best-sell projection remains session-bound; a selected production tree fetches complete depth for its required types instead of retaining the full catalog book. Changing the Main Hub clears the former live projection | Player Structures remain excluded from this public NPC automation; a manual or automatic scan still needs current ESI availability and can retain a previous completed analysis separately from the live projection. Long-session CPU/RSS still requires running-app measurement | `eve-ui-kit` |
| `eve-ui-kit` → `eve-testing-kit` | automatic-data status presentation and performance contract v2 | a compact top-right Dashboard surface independently presents Public Contracts and Main-Hub market phase, region/page progress, partial/failure state, last or next time and a direct pause/resume control for Main-Hub automation. Heavy opportunity analysis runs outside the main actor and cooperatively checks cancellation. Unknown or unconfigured state is not presented as current data | running-app CPU/RSS, narrow-window layout, menu interaction, VoiceOver announcements and a real long-running progress transition remain owner acceptance | `eve-testing-kit` |

## Net Worth 2026-08-04

| Source skill | Source identity and version | Accepted output and target boundary | Open risk | Next skill |
| --- | --- | --- | --- | --- |
| `eve-auth-kit` + `eve-esi-kit` + `eve-assets-kit` + `eve-market-kit` → `eve-ui-kit` | sourced net-worth history contract v2, 2026-08-04 | the dedicated page separates wallet, reference-priced personal assets, remaining sell-order value, ESI-reported buy-order escrow, included items from own active personal item/auction contracts, own in-progress courier collateral and accessible Corporation wallet/assets. Corporation values are deduplicated by Corporation ID. Contract BPCs, requested items, expired contracts, unresolved prices, failed item pages, invalid collateral, missing scopes and missing roles remain explicit and never become zero. Corporation assets require Director; wallets require Accountant or Junior Accountant | existing grants require one browser reauthorization for `esi-contracts.read_character_contracts.v1` and `esi-wallet.read_corporation_wallets.v1`. Current private ESI payloads, multi-day behavior, narrow resizing and EVE-client value comparison remain owner acceptance | `eve-testing-kit` |
| `eve-testing-kit` | synthetic net-worth, private-contract and Corporation-wallet fixtures, 2026-08-04 | SwiftPM and unsigned native Xcode actions each build the app and pass 249 tests in 21 suites. Tests cover missing-scope transport rejection, own/personal/active contract filtering, partial item-page failure, Accountant role enforcement, sell-order value, actual escrow, personal and contract BPC exclusion, courier collateral, Corporation-ID deduplication and backward history decoding | no live ESI refresh or browser reauthorization was initiated; automated evidence does not prove current private-ESI correctness, real role access, cargo value or complete multi-day chart behavior | `eve-update-kit` |
| `eve-auth-kit` + `eve-esi-kit` + `eve-assets-kit` + `eve-market-kit` → `eve-ui-kit` | net-worth diagnosis contract v3, 2026-08-04 | all locally stored grants contain the requested Net Worth scopes, so no scope expansion is needed. Public reference-price gaps are identified by SDE type name; Corporation cards show unique-Corporation coverage and the missing Director or Accountant role count. Incomplete optional Corporation-division labels no longer fail decoding, and missing asset location names no longer make an otherwise complete valuation stale | the current public `/markets/prices/` response covered every locally held non-BPC type during diagnosis, but historical partial points stay immutable. Ingame roles and a new populated snapshot still need owner verification | `eve-testing-kit` |
| `eve-testing-kit` | scope, role, optional-division and wealth-coverage fixtures, 2026-08-04 | isolated SwiftPM and the unsigned native Xcode action each build the app and pass 256 tests in 21 suites. New fixtures cover optional Corporation-division fields, named unpriced types, valuation-specific asset freshness, Corporation role coverage and legacy detail defaults | populated running-app visual acceptance remains open | `eve-update-kit` |

## Canonical persistence and public SDE operator contact 2026-08-05

| Source skill | Source identity and version | Accepted output and target boundary | Open risk | Next skill |
| --- | --- | --- | --- | --- |
| `eve-sde-integration` → `eve-ui-kit` | public operator-configuration contract v2 | the normal SDE UI exposes installed state, update check and installation only. The approved public project address `projekt-st@gmx.de` is compiled into the application, can be overridden by a development environment variable and is normalized before use; an old local setting is read only as a hidden migration fallback | changing the default operator contact requires a source/build update; the public project mailbox must remain monitored and is intentionally not editable by normal users | `eve-testing-kit` |
| `eve-auth-kit` + `eve-update-kit` | canonical application-persistence contract v2 | SwiftData uses the stable application-identifier path and a real V1-to-V2 schema migration; the legacy store is copied without deletion, protected before migration and can merge a newer partial character/settings snapshot once. App-owned defaults use the stable bundle domain and migrate missing executable-domain values | Keychain tokens remain separate and may still require reauthorization when a development build's signing identity changes | `eve-testing-kit` |
| `eve-testing-kit` | protected production-copy recovery, 2026-08-05 | an isolated copy migration and the activated canonical store both pass SQLite integrity checks. The result retains 14 characters, 56 plans, one planner draft, four production rows and the active SDE pointer; all four newer character records were selected during the merge. SwiftPM builds and 260 tests pass; the unsigned native Xcode build passes | populated visual confirmation of character, Profile/market and SDE screens remains owner acceptance | `eve-update-kit` |
