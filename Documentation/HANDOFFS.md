# Skill handoffs

Every row is a handoff envelope. “Version” is the contract or rule version, not
a claim that a live service was exercised.

| Source skill | Source identity and version | Accepted output and target boundary | Open risk | Next skill |
| --- | --- | --- | --- | --- |
| `eve-documentation-kit` | ADR-001; app contract v1; live evidence 2026-07-30 | ownership map, source-state rules and acceptance separation | planner and accessibility owner acceptance remain open | `eve-sde-integration` |
| `eve-sde-integration` | CCP Tranquility build `3451778`; schema changelog checked 2026-07-31 | Level-4 profile for seven JSONL datasets; app owns activation persistence; target adapter derives structures, compatible service modules and rigs plus packaged-volume logistics facts from published types and dogma; update evidence separates active build, official build and latest schema boundary | future schema-changing builds still require the preview and explicit schema gate | `eve-static-data-kit` |
| `eve-static-data-kit` | package identity from vendored skill asset | unchanged package in `Packages/EVEStaticDataKit`; streamed catalog batches and package-derived reaction rule profile | live schema drift remains an update-time gate | `eve-auth-kit` |
| `eve-auth-kit` | EVE SSO v2; callback `http://localhost:52722/callback`; security revision 2026-07-30 | exact one-time loopback callback binding, stored `AuthorizationSnapshot`s and short-lived `AccessTokenLease`s; one reused SSO actor per client ID; the verified SSO character ID automatically reauthorizes its matching local record or adds a new one; Data Protection Keychain with legacy migration/fallback and an explicit local disconnect command; refresh tokens never enter app persistence | prompt-free Data Protection access needs a stable Apple Development identity; EVE requires one browser consent per character during scope renewal; local disconnect does not remotely revoke website access | `eve-esi-kit` |
| `eve-esi-kit` | ESI compatibility `2025-12-16`; CCP Statuspage summary and current asset-location/Image Service guidance reviewed 2026-07-31 | bounded response/pagination handling, non-retried generic POST, a shared cancellable public universe-name index with at most four concurrent name batches, public system/constellation/region details, name-based ACL structure search, known-location discovery from assets/jobs/orders, and ACL-checked structure name/type resolution during asset sync with at most six concurrent detail calls; conditional private caches are character-partitioned, reuse fresh bodies, purge on disconnect and use a 512-entry/64-MiB LRU ceiling; the bounded public status client preserves separate Game Server, Login and ESI states | ESI cannot enumerate every docking ACL or expose facility tax; missing `esi-universe.read_structures.v1`, missing docking ACL and destroyed structures retain ID-labelled partial results; a central error-budget scheduler and long-running live RSS check remain open | `eve-character-kit` |
| `eve-character-kit` | per-character ESI snapshot | identity, skills and unmodified standings with explicit source states; optional capability and personal wallet-balance snapshots persist for cross-character presentation and trader-fee calculation | clone state remains unknown unless explicitly configured | `eve-assets-kit` |
| `eve-assets-kit` | immutable personal asset snapshots; warehouse projection v1; redacted local source-shape audit 2026-07-31 | sourced snapshots persist per character; ESI `other`, structure-range station IDs and unmatched structure-range `item` parents are Player Structure roots, while matching item IDs remain containers; a single item index and bounded roots group location → owner → contents; authenticated structure names/type IDs are frozen into the snapshot while inaccessible names retain their IDs; target composition caches one projection per snapshot identity and resolves SDE names in 500-ID batches | the owner must run **Sync all** once more to refresh snapshots created before orphan-root classification; ACL-inaccessible or destroyed structure names remain explicit IDs; long-running UI RSS remains owner acceptance | `eve-industry-kit` |
| `eve-blueprints-kit` | SDE definition plus sourced ESI owned-instance portfolio; research rule `ccp-blueprint-research-2026-07-v1` | BPO/BPC, owner, location, runs, ME and TE remain endpoint-derived; the target persists complete personal snapshots and joins a selected BPO to its SDE manufacturing/research definition; official level multipliers produce step and cumulative ME/TE quote inputs without creating an executable job | current replacement cost is not historical spend or contract-market value; extra SDE research-material scaling remains blocking until separately verified; concrete invention, copying and research job installation remains unsupported | `eve-market-kit` |
| `eve-market-kit` | The Forge `10000002`, Jita `30000142`, station `60003760`; fee rule `ccp-market-fees-2026-07-02`; private orders live 2026-07-30 | open and historical personal-order snapshots are `fresh` for six characters; order-depth quotes retain separate provenance; public per-type Jita requests use cancellable bounded concurrency of six and ESI cache reuse; one connected trader supplies sourced market fees; complete Jita sell-depth quotes supply logistics collateral without moving haulage economics into Market; public adjusted prices retain snapshot provenance for blueprint research EIV | live rate-limit/error-budget behavior, public Jita order-book and adjusted-price freshness plus owner comparison of the displayed in-game fee remain open | `eve-reactions-kit` |
| `eve-reactions-kit` | SDE reaction activity plus profile rule v1 | reaction candidates without ME/TE; automatic selection ranks refineries with an explicitly configured Reactor service only within the configured reaction system | ESI cannot confirm the live online service-module state | `eve-industry-kit` |
| `eve-assets-kit` → `eve-industry-kit` | combined warehouse policy v1; source snapshots remain immutable | target stock is an Industry-owned protection policy; only factual quantity above target enters allocation, then active reservations are subtracted; manufacturing and reactions share the projection without implying transport; Industry separately values purchased and consumed-stock quantities at current Jita replacement value and includes both in total production cost | concrete Science-job planning, cross-location hauling decisions, historical stock acquisition cost and reservation reconciliation after manual in-game moves remain open | `eve-ui-kit` |
| `eve-blueprints-kit` → `eve-industry-kit` | rules `2026.07-v1`, `ccp-market-fees-2026-07-02` and owner reference `standard-haulage-split-2026-07-30`; SDE facility-reference build `3451778`; spreadsheet reference inspected 2026-07-30 | preserves manual BPC versus BPO provenance and explicit line-level cost treatment; derives runs and facility effects; one physical structure location can serve several module-enabled activities and is ranked independently for each; splits oversized routes into whole-item contracts; composes SDE volume and allocated Jita collateral into separately rounded contracts; freezes blueprint, system index, Facility Tax, SCC, Alpha and logistics costs; derives gross, Sales Tax, Broker Fee and net revenue per sale scenario | automated BPC market valuation, full-BPO amortization policy, concrete Science-job planning, Reprocessing job planning, Thukker special modifiers and ship-class refinement remain open | `eve-ui-kit` |
| `eve-ui-kit` | Assets & Warehouse, expanded Planner, EVE service and SDE update states, 2026-07-31 | compact location rows are fully clickable and initially collapsed; station/structure icon, name, character count and total items precede owner disclosure groups and contents; resolved Upwell type IDs use the official EVE type icon with a generic structure fallback; a global banner distinguishes Tranquility downtime, Login/SSO and ESI, while Characters distinguishes the browser callback from downstream synchronization and reports incomplete domains; Data & Settings saves the normalized SDE owner contact and exposes numeric build/schema evidence | manual accessibility, contact persistence after a real navigation cycle, live banner appearance after downtime, Image Service failure appearance, long-running large-inventory RSS and owner workflow acceptance remain pending | `eve-testing-kit` |
| `eve-testing-kit` | frozen synthetic fixtures and native target test, 2026-07-31 | 116 deterministic tests in 17 suites cover existing contracts plus separate purchased/consumed-stock material valuation, direct and orphan-parent structure classification, matching-container protection, retained player-structure stock, accessible/inaccessible name resolution, a 10,001-item single-pass warehouse fixture, bounded market concurrency, response-cache reuse/LRU/character purge, exact three-character `B9E` matching, shared and four-request-bounded universe-index construction, multi-activity structure selection, optional trader selection before SSO, blueprint portfolio source states and research-cost boundaries, official status-component separation, unknown future status, the UTC downtime fallback, SDE build direction and schema-review boundaries; both the SwiftPM run after a full app build and the unsigned native Xcode test action passed | contact persistence navigation, planner cost-split owner acceptance, post-fix live all-character warehouse population, sustained app-memory measurement and owner visual comparison remain open | `eve-update-kit` |
| `eve-update-kit` | update workflow v1; security revision 2026-07-30; live metadata check 2026-07-31 | check, exact-preview confirmation, explicit schema acknowledgment, backup, staging, validation, activation and rollback coordination; candidates move only to a newer official build and never downgrade an active catalog | destructive replacement is never automatic; disk-space preflight and retention policy remain open | `eve-documentation-kit` |

Partial, forbidden, stale and unresolved inputs generate warnings or block only
the affected result. They never silently become empty or zero-valued data.
