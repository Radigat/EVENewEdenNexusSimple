# Skill handoffs

Every row is a handoff envelope. “Version” is the contract or rule version, not
a claim that a live service was exercised.

| Source skill | Source identity and version | Accepted output and target boundary | Open risk | Next skill |
| --- | --- | --- | --- | --- |
| `eve-documentation-kit` | ADR-001; app contract v1; live evidence 2026-07-30 | ownership map, source-state rules and acceptance separation | planner and accessibility owner acceptance remain open | `eve-sde-integration` |
| `eve-sde-integration` | CCP Tranquility build `3451778`; active local build `3453885` inspected 2026-08-01; schema changelog checked 2026-07-31 | Level-4 profile for seven JSONL datasets; app owns activation persistence; target adapter derives structures, compatible service modules and rigs plus packaged-volume logistics facts from published types and dogma; the Moon purchase analysis consumes exactly the complete published `Moon Materials` group; update evidence separates active build, official build and latest schema boundary | future schema-changing builds still require the preview and explicit schema gate | `eve-static-data-kit` |
| `eve-static-data-kit` | package identity from vendored skill asset | unchanged package in `Packages/EVEStaticDataKit`; streamed catalog batches and package-derived reaction rule profile | live schema drift remains an update-time gate | `eve-auth-kit` |
| `eve-auth-kit` | EVE SSO v2; callback `http://localhost:52722/callback`; security revision 2026-07-30 | exact one-time loopback callback binding, stored `AuthorizationSnapshot`s and short-lived `AccessTokenLease`s; one reused SSO actor per client ID; the verified SSO character ID automatically reauthorizes its matching local record or adds a new one; Data Protection Keychain with legacy migration/fallback and an explicit local disconnect command; refresh tokens never enter app persistence | prompt-free Data Protection access needs a stable Apple Development identity; EVE requires one browser consent per character during scope renewal; local disconnect does not remotely revoke website access | `eve-esi-kit` |
| `eve-esi-kit` | ESI compatibility `2025-12-16`; official ESI/SSO/search guidance reviewed 2026-08-01 | bounded response/pagination handling, non-retried generic POST, a shared cancellable public universe-name index with at most four concurrent name batches, public system/constellation/region details, public NPC-station search with station-owner/faction resolution, name-based ACL structure search both inside a configured production system and globally for Trading Locations, known-location discovery from assets/jobs/orders, and ACL-checked structure name/type resolution during asset sync with at most six concurrent detail calls; conditional private caches are character-partitioned, reuse fresh bodies, purge on disconnect and use a 512-entry/64-MiB LRU ceiling; the bounded public status client preserves separate Game Server, Login and ESI states | ESI cannot enumerate every docking ACL or expose a Player Structure's owner-defined broker fee or facility tax; missing `esi-universe.read_structures.v1`, missing docking ACL and destroyed structures retain ID-labelled partial results; a central error-budget scheduler and long-running live RSS check remain open | `eve-character-kit` |
| `eve-character-kit` | per-character ESI snapshot | identity, skills and unmodified standings with explicit source states; optional capability and personal wallet-balance snapshots persist for cross-character presentation and trader-fee calculation | clone state remains unknown unless explicitly configured | `eve-assets-kit` |
| `eve-assets-kit` | immutable personal asset snapshots; warehouse projection v4; redacted local source-shape audit 2026-07-31 | sourced snapshots persist per character; ESI `other`, structure-range station IDs and unmatched structure-range `item` parents are Player Structure roots, while matching item IDs remain containers; a single item index and bounded roots group location → owner → contents and retain ancestor type IDs; authenticated structure names/type IDs are frozen into the snapshot while inaccessible names retain their IDs; target composition caches one projection per snapshot identity, resolves SDE names in 500-ID batches, derives a provenance-preserving production view for exact configured facility IDs while excluding both SDE category `Ship` and every ship descendant, pre-aggregates display rows by owner/type/raw inventory flag, and supplies a deterministic organizer for alphabetical, SDE-group and SDE-category/main-group sections with an explicit unresolved fallback | Corporation hangars remain explicitly unavailable because corporation-asset authorization, role capability and snapshots are not implemented; the owner must run **Sync all** once more to refresh older snapshots; ACL-inaccessible or destroyed structure names remain explicit IDs; long-running UI RSS remains owner acceptance | `eve-industry-kit` |
| `eve-blueprints-kit` | SDE definition plus sourced ESI owned-instance portfolio; research rule `ccp-blueprint-research-2026-07-v1` | BPO/BPC, owner, location, runs, ME and TE remain endpoint-derived; the target persists complete personal snapshots and joins a selected BPO to its SDE manufacturing/research definition; official level multipliers produce step and cumulative ME/TE quote inputs without creating an executable job | current replacement cost is not historical spend or contract-market value; extra SDE research-material scaling remains blocking until separately verified; concrete invention, copying and research job installation remains unsupported | `eve-market-kit` |
| `eve-market-kit` | ESI compatibility `2025-12-16`; NPC hub region/station IDs; UALX-3 and C-J6MT system/region plus time-sensitive structure IDs verified 2026-08-01; fee rule `ccp-market-fees-2026-07-02` | Planner requests location-bound order snapshots for the Profile Main Hub and prices exact required quantity through sell depth; Main Hub and independent Home Hub persist separately; each Trading Location owns its trader and fee evidence, with NPC broker fees bound to the selected character plus station owner/faction context; catalog-wide reaction analysis retains its independent selectable hub; Moon purchase analysis compares six exact locations; the cross-region Market Browser requests one item's public orders from every ESI region with bounded concurrency, preserves partial region failures, resolves location/system labels, retains regional dataset timestamps, and optionally adds bounded route distances without converting unchecked or unreachable routes into zero | current ESI market-order rate limits and caching must be respected; player-structure docking ACLs and owner-defined broker fees are unavailable; `Last-Modified` is regional dataset provenance rather than guaranteed per-order edit time; active-order volume is not historical completed-sale volume; live all-region and visual owner acceptance remain open | `eve-reactions-kit` |
| `eve-reactions-kit` | active SDE build plus reaction rule `2026.07-v1`; run-limit clarification 2026-08-01 | every complete published reaction formula is analyzed for persisted runs (default 100), input cost, output replacement value, immediate-sale spread, make-or-buy savings, value status and margin; the one-job run maximum is derived from the 30-day window, SDE duration and accepted time multiplier, with a minimum of one run and an explicit required-job count | live ESI prices are time-bound; missing depth remains unavailable; ESI cannot confirm the live online reactor service and owner comparison with the in-game reaction window remains open | `eve-industry-kit` |
| `eve-assets-kit` → `eve-industry-kit` | combined warehouse policy v2; source snapshots remain immutable | target stock is an Industry-owned protection policy; only factual quantity above target enters availability, then active reservations are subtracted; Industry consumes it only for a raw material or intermediate explicitly set to warehouse; any shortfall remains a purchase; actual stock consumption receives an informational countervalue from the same full-requirement weighted Main Hub unit price without mutating the source snapshot or adding the value twice | concrete Science-job planning, historical stock acquisition cost and reservation reconciliation after manual in-game moves remain open | `eve-ui-kit` |
| `eve-blueprints-kit` → `eve-industry-kit` | rules `2026.07-v1`, `ccp-market-fees-2026-07-02` and procurement logistics `main-hub-to-production-2026-08-01`; SDE facility-reference build `3451778`; spreadsheet reference inspected 2026-07-30 | preserves manual BPC versus BPO provenance and explicit line-level cost treatment; derives runs and facility effects; every producible component and reaction intermediate receives a full-quantity make-or-buy analysis with Main Hub depth, batch-rounded direct inputs, installation and inbound logistics; applied preferences control the actual graph; each resulting `IndustryJobCost` freezes product, runs, output quantity, ME/TE, top-level status, facility and installation cost for the build list; all purchases use the Profile Main Hub | automated BPC market valuation, full-BPO amortization policy, concrete Science-job planning, Reprocessing job planning, Thukker special modifiers and ship-class refinement remain open | `eve-ui-kit` |
| `eve-ui-kit` | shared disclosure, warehouse, Planner summary, Moon comparison, Market Browser and production-book interaction revision 2026-08-01 | The new Market Browser navigation entry provides three-character active-SDE item search, a separate route-origin picker, summary metrics, explicit partial-source status, sortable Sellers and Buyers tables, and region/location/price/quantity/jump/location-kind filters. Buyer rows include range and minimum volume; seller and buyer rows retain expiry, regional ESI update time and explicit unchecked/unreachable route states. Existing disclosure, Warehouse, Planner, Moon comparison and production-book contracts remain unchanged | running-app resizing, keyboard/VoiceOver, long all-region load, player-structure wording and comparison with current in-game orders remain owner acceptance; historical completed-sale volume is not yet presented | `eve-testing-kit` |
| `eve-testing-kit` | frozen synthetic fixtures and native target test, 2026-08-01 | 156 deterministic tests in 19 suites pass through both SwiftPM and the unsigned native Xcode test action; four focused Market Browser tests prove partial region retention, field and provenance mapping, resolved location/system names, bounded route behavior, explicit unknown routes, filtering and volume-weighted active-sell summary semantics | live all-region ESI compatibility, long-running rate-limit/cache behavior, running-app layout and historical market-volume coverage remain open | `eve-update-kit` |
| `eve-update-kit` | update workflow v1; security revision 2026-07-30; live metadata check 2026-07-31 | check, exact-preview confirmation, explicit schema acknowledgment, backup, staging, validation, activation and rollback coordination; candidates move only to a newer official build and never downgrade an active catalog | destructive replacement is never automatic; disk-space preflight and retention policy remain open | `eve-documentation-kit` |

Partial, forbidden, stale and unresolved inputs generate warnings or block only
the affected result. They never silently become empty or zero-valued data.
